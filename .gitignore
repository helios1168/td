__pycache__/
*.pyc
*.pyo
.DS_Store

# LaTeX build residue
*.aux
*.log
*.out
*.bbl
*.blg
*.synctex.gz
*.fdb_latexmk
*.fls

# Local environments
.venv/
venv/
.claude/.headroom_wrap_settings.lock

# Claude Code worktrees (nested checkouts, never commit)
.claude/worktrees/

# Contiguity programme data (PLAN.md U0c): TIGER download and the synthetic twin
data/tiger/
data/twin_*
data/public/

# Census gazetteer / cartographic-boundary cache (td/geo.py).  The cache is ignored; the maps
# built from it are NOT -- `figures/` is committed, because a figure is a primary artifact and
# a claim in the docs that nobody can see is worse than 3 MB of PNG.  Regenerate with
# `tools/us_maps.py` (the header names the flags) and commit the result alongside the change.
data/geo/

# Contiguity harness output (PLAN.md C.1): rows.jsonl, assignments, instance JSONs
battery/results/

# editor scratch
*.swp
instance_descaled.json.gz

# helios track charter (per worktree, written by tools/track-setup.sh)
CLAUDE.local.md
