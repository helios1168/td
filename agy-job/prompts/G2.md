# G2 mathematical gate: reservation and centered state-stage matching

Read-only mathematical review. Do not edit files, create files, commit, or push. The target is
`/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review` on branch
`worktree-agy-math-review`.

Read:

- `IMPLEMENTATION_PLAN.md`, sections 3, 6-12.
- `agy-job/contract.md`, sections 3-7, 9, 10, and 14.
- `agy-job/reports/GATE.json`, `agy-job/reports/A1.json`, `agy-job/reports/A2.json`, and
  `agy-job/reports/R1.json`, `agy-job/reports/R2.json`.
- `td/stage2_state.py`, `tests/test_stage2_state.py`, `tools/full_plan.py`, and
  `tests/test_full_plan_cli.py`.

The accepted contract is authoritative. This is a gate over the implementation and evidence, not
an invitation to reopen E1-E7 or invent a strict-convexity theorem. Check the code against these
settled claims:

1. `compute_reservation_vector` uses gain units and exactly
   `d_floor=gamma*G0_floor`, `none=0`, `uniform=d_floor`, and
   `claims=alpha*S_book+d_floor` with
   `alpha=min(1,region_M/region_T)*(1-epsilon)` and zero-region guards. Inputs and outputs reject
   invalid non-finite or negative quantities as documented.
2. State-stage matching uses the raw gain matrix, centered surplus `g-d_i`, strict positive mask,
   no clipping of forbidden edges, the masked Hungarian path, and rejects any Hall shortfall. A
   successful result reports centered gains, zero clipped edges, nonpositive edges, zero Hall
   shortfall, state grain, and the exact report-only epsilon floor when the caller supplies it.
3. Rectangular matching, candidacy, empty plans, and legacy `reservation=None` behavior remain
   sound. The assignment is one rep per used slot, and no more than one slot per rep.
4. `_compute_reservation_for_plan` in `tools/full_plan.py` computes state-grain ambient
   `G0_floor` using the same coefficient formula as `state_utilities`, computes the full-region
   book and opportunity quantities, passes the exact floor, and recomputes reservations for trial
   plans. The CLI defaults and valid `none` behavior preserve geometry and legacy values.
5. The replacement checks are P1 positive matched surplus, P2 complete positive matching, P3 the
   total-unimodular assignment polytope, and P4 conditioning near the reservation boundary. Do
   not assess strict convexity, because the contract explicitly dropped that category error.

Try to falsify the claims using independent hand calculations or brute force on small synthetic
arrays and by inspecting the focused tests. Confirm the known environmental limits: pytest is
absent, the repository runner is the executable test path, and the confidential CONUS instance,
geo cache, and ZCTA shapefile are absent, so no CONUS benchmark or real-map FEFx count can be
claimed.

Return a concise plain-text memo with:

- a verdict for each numbered claim: APPROVED, REFUTED, or INCONCLUSIVE;
- the independent calculations or concrete counterexamples used;
- whether R1 and R2 are authorized to proceed to M1;
- any required repair, with exact file and symbol, if a claim is not approved;
- an explicit statement that this is not final V1 sign-off.
