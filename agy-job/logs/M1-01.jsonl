{"type":"step_start","timestamp":1789350526316,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"id":"prt_09d9a3d6b001m4mCSsx62WI83S","messageID":"msg_09d9a3500001I9VIVZzS4ltR3M","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","snapshot":"76f13906b004b9e7cf0c352b965f9312de7b8b17","type":"step-start"}}
{"type":"tool_use","timestamp":1789350537699,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"type":"tool","tool":"read","callID":"call_00_yo7vhsht4mAM70yPwMdI5795","state":{"status":"completed","input":{"filePath":"/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/td/stage2_state.py"},"output":"<path>/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/td/stage2_state.py</path>\n<type>file</type>\n<content>\n1: \"\"\"stage2_state.py -- stage 2 (Nash staffing) at state x channel grain.\n2: \n3: The level-0 plan decides, per state, which bundles of channels are served: keep three\n4: channels, merge WH and FI, or drop national.  A move like that changes what every rep can\n5: be handed, so it has to be priced by the staffing it enables, not by mass alone.  Pricing it\n6: at zip level would mean re-drawing the map for every candidate move; pricing it here does not,\n7: because the utility is additive over cells:\n8: \n9:     u_i(s, c) = c1*S_i(s,c) + c2*(T_{s,c} - S_i(s,c)) + c_free*S_free(s,c) + lam*M_{s,c}\n10: \n11: summing the zips of a state gives exactly the state-level cell, and summing the cells a slot\n12: holds gives that slot's gain.  So the state-level gain of a rep on a slot is\n13: `sum_s y_j[s] * sum_{c in B_j} u_i(s,c)`, and the Nash match over slots is the same Hungarian\n14: `channel.match` runs over districts.  On whole states the score is exact; on a state split\n15: between slots it is the mass-proportional approximation, and level 2 decides which zips move.\n16: \n17: `cells` is a `channels.CellTable`: `state_list`, `channels`, `reps`, `M (S,C)`,\n18: `S (R,S,C)`, `S_free (S,C)`.  It is read by attribute, never imported, so this module stands\n19: alone.  Rows of `S` follow `cells.reps`; columns follow `cells.channels`.\n20: \n21: Candidacy (`candidacy=True`) restricts a rep to slots where it holds book by *masking the\n22: match*, never by releasing anyone: a released rep's book moves into `S_free`, which every\n23: other candidate then prices at `c_free`, and that distorts the valuation of the whole\n24: neighbourhood (CLAUDE.md trap 20).\n25: \"\"\"\n26: from __future__ import annotations\n27: \n28: import math\n29: from dataclasses import dataclass, field\n30: \n31: import numpy as np\n32: from scipy.optimize import linear_sum_assignment\n33: \n34: from td import channel, model\n35: \n36: \n37: _RESERVATION_MODES = (\"none\", \"uniform\", \"claims\")\n38: POSITIVITY_TOL = 0.0        # strict surplus: an allowed centered edge is exactly `> 0`\n39: \n40: \n41: def compute_reservation_vector(\n42:         S_book: np.ndarray,\n43:         *,\n44:         region_M: float,\n45:         region_T: float,\n46:         G0_floor: float,\n47:         mode: str = \"none\",\n48:         gamma: float = 0.60,\n49:         epsilon: float = 0.05,\n50: ) -> np.ndarray:\n51:     \"\"\"Per-rep reservation `d_i` (descaled gain units) for one of three modes.\n52: \n53:     `d_floor = gamma * G0_floor` is the common ambient floor.  \"none\" is the legacy `d = 0`;\n54:     \"uniform\" gives every rep `d_floor`; \"claims\" adds `alpha * S_book[i]` on top of the floor,\n55:     where `alpha = min(1, region_M / region_T) * (1 - epsilon)` when both region quantities are\n56:     positive and 0 otherwise.  `S_book` has one entry per rep in `cells.reps` order.  Returns a\n57:     fresh float array and never mutates the input.\n58:     \"\"\"\n59:     if mode not in _RESERVATION_MODES:\n60:         raise ValueError(f\"reservation mode {mode!r} not in {_RESERVATION_MODES}\")\n61:     if not (0.0 <= gamma < 1.0):\n62:         raise ValueError(f\"gamma {gamma!r} must be in [0, 1)\")\n63:     if not (0.0 <= epsilon < 1.0):\n64:         raise ValueError(f\"epsilon {epsilon!r} must be in [0, 1)\")\n65:     S = np.asarray(S_book, float)\n66:     if S.ndim != 1:\n67:         raise ValueError(f\"S_book must be one-dimensional, got shape {S.shape}\")\n68:     if not np.isfinite(S).all():\n69:         raise ValueError(\"S_book must be finite\")\n70:     if (S < 0.0).any():\n71:         raise ValueError(\"S_book must be nonnegative\")\n72:     region_M = _nonneg_scalar(\"region_M\", region_M)\n73:     region_T = _nonneg_scalar(\"region_T\", region_T)\n74:     G0_floor = _nonneg_scalar(\"G0_floor\", G0_floor)\n75:     d_floor = gamma * G0_floor\n76:     if mode == \"none\":\n77:         d = np.zeros(S.shape, float)\n78:     elif mode == \"uniform\":\n79:         d = np.full(S.shape, d_floor, float)\n80:     else:  # \"claims\"\n81:         alpha = (min(1.0, region_M / region_T) * (1.0 - epsilon)\n82:                  if region_M > 0.0 and region_T > 0.0 else 0.0)\n83:         d = alpha * S + d_floor\n84:     if not np.isfinite(d).all():\n85:         raise ValueError(\"reservation vector is not finite\")\n86:     return d\n87: \n88: \n89: def _nonneg_scalar(name: str, val) -> float:\n90:     v = np.asarray(val, float)\n91:     if v.ndim != 0:\n92:         raise ValueError(f\"{name} must be a scalar, got shape {v.shape}\")\n93:     x = float(v)\n94:     if not math.isfinite(x):\n95:         raise ValueError(f\"{name} must be finite\")\n96:     if x < 0.0:\n97:         raise ValueError(f\"{name} must be nonnegative\")\n98:     return x\n99: \n100: \n101: def _validate_reservation(reservation, n_reps: int) -> np.ndarray:\n102:     d = np.asarray(reservation, float)\n103:     if d.ndim != 1:\n104:         raise ValueError(f\"reservation must be one-dimensional, got shape {d.shape}\")\n105:     if d.shape[0] != n_reps:\n106:         raise ValueError(f\"reservation has {d.shape[0]} entries for {n_reps} reps\")\n107:     if not np.isfinite(d).all():\n108:         raise ValueError(\"reservation must be finite\")\n109:     if (d < 0.0).any():\n110:         raise ValueError(\"reservation must be nonnegative\")\n111:     return d\n112: \n113: \n114: @dataclass\n115: class Slot:\n116:     \"\"\"One level-0 slot: a bundle of channels and the share of each state it holds.\"\"\"\n117:     bundle: tuple[str, ...]\n118:     y: dict = field(default_factory=dict)      # state -> share in [0, 1]\n119:     used: bool = True\n120: \n121: \n122: @dataclass\n123: class Plan:\n124:     \"\"\"A channel plan: the slots and the state order the level-0 model used.\n125: \n126:     `state_list` is informational here.  Every slot's `y` is keyed by state *name* and is\n127:     looked up in `cells.state_list`, so the two orders need not agree.\n128:     \"\"\"\n129:     slots: list\n130:     state_list: list = field(default_factory=list)\n131: \n132: \n133: # ------------------------------------------------------------------ index helpers\n134: def _rep_rows(cells, reps) -> list:\n135:     \"\"\"Row indices into `cells.S` for `reps` (all of them, in table order, when None).\"\"\"\n136:     if reps is None:\n137:         return list(range(len(cells.reps)))\n138:     idx = {r: i for i, r in enumerate(cells.reps)}\n139:     missing = [r for r in reps if r not in idx]\n140:     if missing:\n141:         raise ValueError(f\"rep(s) {missing} are not in the cell table\")\n142:     return [idx[r] for r in reps]\n143: \n144: \n145: def _channel_cols(cells, bundle) -> list:\n146:     \"\"\"Column indices into the channel axis for the channels of `bundle`.\"\"\"\n147:     idx = {c: j for j, c in enumerate(cells.channels)}\n148:     missing = [c for c in bundle if c not in idx]\n149:     if missing:\n150:         raise ValueError(f\"channel(s) {missing} are not in the cell table {cells.channels}\")\n151:     return [idx[c] for c in bundle]\n152: \n153: \n154: def _y_vector(cells, y) -> np.ndarray:\n155:     \"\"\"A slot's state shares as a dense vector over `cells.state_list`.\"\"\"\n156:     idx = {s: i for i, s in enumerate(cells.state_list)}\n157:     vec = np.zeros(len(cells.state_list), float)\n158:     for name, share in y.items():\n159:         if name not in idx:\n160:             raise ValueError(f\"state {name!r} is not in the cell table\")\n161:         vec[idx[name]] = float(share)\n162:     return vec\n163: \n164: \n165: def _slot_sums(cells, plan, slot_ids, arr: np.ndarray) -> np.ndarray:\n166:     \"\"\"`(R, J)`: `arr` summed over each slot's bundle and weighted by its state shares.\"\"\"\n167:     out = np.zeros((arr.shape[0], len(slot_ids)), float)\n168:     for col, j in enumerate(slot_ids):\n169:         slot = plan.slots[j]\n170:         cols = _channel_cols(cells, slot.bundle)\n171:         out[:, col] = arr[:, :, cols].sum(2) @ _y_vector(cells, slot.y)\n172:     return out\n173: \n174: \n175: def _slot_mass(cells, plan, slot_ids) -> np.ndarray:\n176:     \"\"\"Opportunity held by each used slot.\"\"\"\n177:     M = np.asarray(cells.M, float)\n178:     vals = [M[:, _channel_cols(cells, plan.slots[j].bundle)].sum(1)\n179:             @ _y_vector(cells, plan.slots[j].y) for j in slot_ids]\n180:     return np.array(vals, float)\n181: \n182: \n183: # ------------------------------------------------------------------- the utility\n184: def state_utilities(cells, reps=None, *, theta: float = 0.40, lam: float = 0.30,\n185:                     filler_capture: str = \"theta\") -> np.ndarray:\n186:     \"\"\"`u[i, s, c]` for the reps of `reps` (all of them when None), in that order.\n187: \n188:     `T_{s,c}` is a property of the cell, so it sums the book of *every* rep in the table even\n189:     when `reps` selects a subset: a rep left out of this subproblem still holds its book.\n190:     \"\"\"\n191:     c1, c2, c_free = model.coefficients(theta, lam, filler_capture)\n192:     S_all = np.asarray(cells.S, float)\n193:     T = S_all.sum(0)                                    # (S, C), over every rep\n194:     common = (c2 * T + c_free * np.asarray(cells.S_free, float)\n195:               + lam * np.asarray(cells.M, float))\n196:     return common[None, :, :] + (c1 - c2) * S_all[_rep_rows(cells, reps)]\n197: \n198: \n199: def state_gain_matrix(cells, plan, *, reps=None, theta: float = 0.40, lam: float = 0.30,\n200:                       filler_capture: str = \"theta\",\n201:                       reservation: np.ndarray | None = None):\n202:     \"\"\"`(g, reps, slot_ids)` over the *used* slots: `g[i, j]` is rep i's gain from slot j.\n203: \n204:     `slot_ids` are indices into `plan.slots`; an unused slot has no column and so can never\n205:     be matched.  This is `channel.gain_matrix` aggregated: same coefficients, same \"every rep\n206:     is priced on every slot\" convention (candidacy is a stage-2 restriction, not a stage-1 one).\n207: \n208:     With `reservation` (one gain-unit entry per rep, in `R` order) the returned matrix is the\n209:     centered surplus `g - d_i`, not the raw gain; without it the raw gain is returned.\n210:     \"\"\"\n211:     R = list(cells.reps) if reps is None else list(reps)\n212:     slot_ids = [j for j, slot in enumerate(plan.slots) if slot.used]\n213:     u = state_utilities(cells, reps, theta=theta, lam=lam, filler_capture=filler_capture)\n214:     g = _slot_sums(cells, plan, slot_ids, u)\n215:     if reservation is not None:\n216:         d = _validate_reservation(reservation, len(R))\n217:         g = g - d[:, None]\n218:     return g, R, slot_ids\n219: \n220: \n221: # ---------------------------------------------------------------------- matching\n222: def _match_masked(g: np.ndarray, ok: np.ndarray, criterion: str):\n223:     \"\"\"`channel.match` restricted to the pairs `ok` allows.\n224: \n225:     `linear_sum_assignment` matches `min(rows, cols)` cells whatever the costs, so forbidden\n226:     cells cannot simply be dropped: they are priced above anything a swap of allowed cells can\n227:     recover, then filtered out of the result.  This is `tools/staff.py`'s `assign`.\n228:     \"\"\"\n229:     if criterion == \"nash\":\n230:         ok = ok & (g > 0.0)        # a zero gain is no staffing, as in `tools/staff.py`\n231:     if g.size == 0 or not ok.any():\n232:         return [], 0.0\n233:     if criterion == \"nash\":\n234:         with np.errstate(divide=\"ignore\", invalid=\"ignore\"):\n235:             cost = -np.log(np.where(ok, g, 1.0))\n236:     elif criterion == \"utilitarian\":\n237:         cost = -np.where(ok, g, 0.0)\n238:     else:\n239:         raise ValueError(f\"criterion {criterion!r} not in ('nash', 'utilitarian')\")\n240:     lo, hi = float(cost[ok].min()), float(cost[ok].max())\n241:     pen = hi + (min(g.shape) + 1) * (hi - lo + 1.0)\n242:     rows, cols = linear_sum_assignment(np.where(ok, cost, pen))\n243:     pairs = [(int(i), int(j)) for i, j in zip(rows, cols) if ok[i, j]]\n244:     value = float(sum(math.log(g[i, j]) if criterion == \"nash\" else g[i, j]\n245:                       for i, j in pairs))\n246:     return pairs, value\n247: \n248: \n249: def _check_staffable(g: np.ndarray, ok: np.ndarray, plan, slot_ids) -> None:\n250:     \"\"\"Every used slot must have at least one candidate with a positive gain.\"\"\"\n251:     for col, j in enumerate(slot_ids):\n252:         if not (ok[:, col] & (g[:, col] > 0.0)).any():\n253:             raise ValueError(\n254:                 f\"slot {j} (bundle {tuple(plan.slots[j].bundle)}) has no candidate rep with a \"\n255:                 f\"positive gain, so it cannot be staffed; drop the slot or widen candidacy\")\n256: \n257: \n258: def _balance(vals: np.ndarray, target=None) -> dict:\n259:     \"\"\"`channel.balance_report`'s fields from the slot masses.\"\"\"\n260:     if vals.size == 0:\n261:         return dict(k=0)\n262:     tgt = float(np.mean(vals)) if target is None else float(target)\n263:     return dict(\n264:         k=int(vals.size),\n265:         total=float(vals.sum()),\n266:         target=tgt,\n267:         mean=float(vals.mean()),\n268:         min=float(vals.min()),\n269:         max=float(vals.max()),\n270:         spread_rel=float((vals.max() - vals.min()) / vals.mean()) if vals.mean() else 0.0,\n271:         max_dev_rel=float(np.abs(vals - tgt).max() / tgt) if tgt else 0.0,\n272:         log_sum=float(np.log(vals).sum()) if (vals > 0).all() else -math.inf,\n273:     )\n274: \n275: \n276: def state_stage2(cells, plan, *, reps=None, theta: float = 0.40, lam: float = 0.30,\n277:                  filler_capture: str = \"theta\", criterion: str = \"nash\",\n278:                  candidacy: bool = False,\n279:                  reservation: np.ndarray | None = None,\n280:                  reservation_floor: float | None = None,\n281:                  reservation_mode: str = \"none\") -> dict:\n282:     \"\"\"Staff a channel plan at state grain.  Same keys as `channel.stage2`.\n283: \n284:     `districts` holds the used slots' indices into `plan.slots`, so `assignment` and `gains`\n285:     are keyed by slot id.  With `candidacy=False` this is `channel.match` verbatim, which\n286:     refuses a non-positive gain anywhere; with `candidacy=True` a rep is only offered the\n287:     slots where it holds book, and the match is masked rather than the instance changed.\n288: \n289:     With `reservation` (one gain-unit entry per rep, in `R` order) the match is run on the raw\n290:     centered surplus `g - d_i`, never on a clipped matrix: an edge is allowed only when its\n291:     centered surplus is strictly positive, and a matching that fails to saturate every used\n292:     slot raises (a Hall shortfall).  `gains` are then the matched centered surpluses and\n293:     `value`/`value_centered` are computed from them.  `reservation=None` is the legacy `d=0`\n294:     anchor and keeps the raw gains and legacy value.\n295: \n296:     `reservation_floor` supplies the exact common ambient floor `d_floor = gamma * G0_floor`\n297:     used by the report-only `epsilon_floor = max(1e-6 * d_floor, 1e-12)`.  Without it the\n298:     floor is recovered as `min(reservation)`, which is exact for uniform mode and for claims\n299:     mode with at least one zero-claim rep but overstates `d_floor` in claims mode when every\n300:     rep holds positive book.  The floor is report-only: it never admits or clips an assignment\n301:     edge.\n302:     \"\"\"\n303:     if reservation_mode not in _RESERVATION_MODES:\n304:         raise ValueError(f\"reservation_mode {reservation_mode!r} not in {_RESERVATION_MODES}\")\n305:     if reservation_floor is not None:\n306:         reservation_floor = _nonneg_scalar(\"reservation_floor\", reservation_floor)\n307:     g, R, slot_ids = state_gain_matrix(cells, plan, reps=reps, theta=theta, lam=lam,\n308:                                        filler_capture=filler_capture)\n309:     centered = g\n310:     d_floor = 0.0\n311:     mode = \"none\"\n312:     if reservation is not None:\n313:         d = _validate_reservation(reservation, len(R))\n314:         centered = g - d[:, None]\n315:         if reservation_floor is not None:\n316:             d_floor = reservation_floor\n317:         else:\n318:             # Fallback for callers that supply only the raw reservation vector: exact for\n319:             # uniform mode and for claims mode with a zero-claim rep, but overstates d_floor\n320:             # in claims mode when every rep holds positive book.\n321:             d_floor = float(d.min()) if d.size else 0.0\n322:         mode = reservation_mode\n323:     ok = np.ones(centered.shape, bool)\n324:     if candidacy:\n325:         book = _slot_sums(cells, plan, slot_ids,\n326:                           np.asarray(cells.S, float)[_rep_rows(cells, reps)])\n327:         ok = book > 0.0\n328:     if reservation is not None:\n329:         ok = ok & (centered > POSITIVITY_TOL)\n330:         pairs, value = _match_masked(centered, ok, criterion)\n331:         short = [slot_ids[j] for j in range(len(slot_ids))\n332:                  if j not in {j_ for _, j_ in pairs}]\n333:         if short:\n334:             raise ValueError(\n335:                 f\"{len(short)} used slot(s) {short} cannot be staffed under the reservation: \"\n336:                 f\"the centered-surplus candidate sets of the used slots have no system of \"\n337:                 f\"distinct representatives (Hall shortfall); lower the reservation or widen \"\n338:                 f\"candidacy\")\n339:     else:\n340:         _check_staffable(g, ok, plan, slot_ids)\n341:         pairs, value = (_match_masked(g, ok, criterion) if candidacy\n342:                         else channel.match(g, criterion))\n343:         if candidacy and len(pairs) < len(slot_ids):\n344:             # `_check_staffable` is per column, so it passes when two slots share their only\n345:             # candidate: the shortfall is Hall's condition, not an empty column, and only the\n346:             # matching can see it.  A rep can hold one slot, so the plan is not staffable.\n347:             short = [slot_ids[j] for j in range(len(slot_ids))\n348:                      if j not in {j_ for _, j_ in pairs}]\n349:             raise ValueError(\n350:                 f\"slot(s) {short} cannot be staffed under candidacy: the candidate sets of the \"\n351:                 f\"used slots have no system of distinct representatives; drop a slot or widen \"\n352:                 f\"candidacy\")\n353:     assign = {slot_ids[j]: R[i] for i, j in pairs}\n354:     gains = {slot_ids[j]: float(centered[i, j]) for i, j in pairs}\n355:     matched = {R[i] for i, _ in pairs}\n356:     return dict(\n357:         assignment=assign,                       # slot id -> rep\n358:         gains=gains,                             # slot id -> that rep's (centered) surplus\n359:         value=value,                             # sum log g (nash) or sum g (utilitarian)\n360:         value_centered=value,                    # == value: this module's matrix is the center\n361:         criterion=criterion,\n362:         reps=R, districts=slot_ids,\n363:         unmatched_reps=[r for r in R if r not in matched],\n364:         unstaffed_districts=[j for j in slot_ids if j not in assign],\n365:         balance=_balance(_slot_mass(cells, plan, slot_ids)),\n366:         reservation_mode=mode,\n367:         reservation_grain=\"state\",\n368:         clipped_edges=0,\n369:         nonpositive_edges=int((centered <= 0.0).sum()),\n370:         hall_shortfall=0,\n371:         epsilon_floor=max(1e-6 * d_floor, 1e-12),\n372:     )\n\n(End of file - total 372 lines)\n</content>","metadata":{"preview":"\"\"\"stage2_state.py -- stage 2 (Nash staffing) at state x channel grain.\n\nThe level-0 plan decides, per state, which bundles of channels are served: keep three\nchannels, merge WH and FI, or drop national.  A move like that changes what every rep can\nbe handed, so it has to be priced by the staffing it enables, not by mass alone.  Pricing it\nat zip level would mean re-drawing the map for every candidate move; pricing it here does not,\nbecause the utility is additive over cells:\n\n    u_i(s, c) = c1*S_i(s,c) + c2*(T_{s,c} - S_i(s,c)) + c_free*S_free(s,c) + lam*M_{s,c}\n\nsumming the zips of a state gives exactly the state-level cell, and summing the cells a slot\nholds gives that slot's gain.  So the state-level gain of a rep on a slot is\n`sum_s y_j[s] * sum_{c in B_j} u_i(s,c)`, and the Nash match over slots is the same Hungarian\n`channel.match` runs over districts.  On whole states the score is exact; on a state split\nbetween slots it is the mass-proportional approximation, and level 2 decides which zips move.\n\n`cells` is a `channels.CellTable`: `state_list`, `channels`, `reps`, `M (S,C)`,\n`S (R,S,C)`, `S_free (S,C)`.  It is read by attribute, never imported, so this module stands\nalone.  Rows of `S` follow `cells.reps`; columns follow `cells.channels`.\n","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/td/stage2_state.py","text":"\"\"\"stage2_state.py -- stage 2 (Nash staffing) at state x channel grain.\n\nThe level-0 plan decides, per state, which bundles of channels are served: keep three\nchannels, merge WH and FI, or drop national.  A move like that changes what every rep can\nbe handed, so it has to be priced by the staffing it enables, not by mass alone.  Pricing it\nat zip level would mean re-drawing the map for every candidate move; pricing it here does not,\nbecause the utility is additive over cells:\n\n    u_i(s, c) = c1*S_i(s,c) + c2*(T_{s,c} - S_i(s,c)) + c_free*S_free(s,c) + lam*M_{s,c}\n\nsumming the zips of a state gives exactly the state-level cell, and summing the cells a slot\nholds gives that slot's gain.  So the state-level gain of a rep on a slot is\n`sum_s y_j[s] * sum_{c in B_j} u_i(s,c)`, and the Nash match over slots is the same Hungarian\n`channel.match` runs over districts.  On whole states the score is exact; on a state split\nbetween slots it is the mass-proportional approximation, and level 2 decides which zips move.\n\n`cells` is a `channels.CellTable`: `state_list`, `channels`, `reps`, `M (S,C)`,\n`S (R,S,C)`, `S_free (S,C)`.  It is read by attribute, never imported, so this module stands\nalone.  Rows of `S` follow `cells.reps`; columns follow `cells.channels`.\n\nCandidacy (`candidacy=True`) restricts a rep to slots where it holds book by *masking the\nmatch*, never by releasing anyone: a released rep's book moves into `S_free`, which every\nother candidate then prices at `c_free`, and that distorts the valuation of the whole\nneighbourhood (CLAUDE.md trap 20).\n\"\"\"\nfrom __future__ import annotations\n\nimport math\nfrom dataclasses import dataclass, field\n\nimport numpy as np\nfrom scipy.optimize import linear_sum_assignment\n\nfrom td import channel, model\n\n\n_RESERVATION_MODES = (\"none\", \"uniform\", \"claims\")\nPOSITIVITY_TOL = 0.0        # strict surplus: an allowed centered edge is exactly `> 0`\n\n\ndef compute_reservation_vector(\n        S_book: np.ndarray,\n        *,\n        region_M: float,\n        region_T: float,\n        G0_floor: float,\n        mode: str = \"none\",\n        gamma: float = 0.60,\n        epsilon: float = 0.05,\n) -> np.ndarray:\n    \"\"\"Per-rep reservation `d_i` (descaled gain units) for one of three modes.\n\n    `d_floor = gamma * G0_floor` is the common ambient floor.  \"none\" is the legacy `d = 0`;\n    \"uniform\" gives every rep `d_floor`; \"claims\" adds `alpha * S_book[i]` on top of the floor,\n    where `alpha = min(1, region_M / region_T) * (1 - epsilon)` when both region quantities are\n    positive and 0 otherwise.  `S_book` has one entry per rep in `cells.reps` order.  Returns a\n    fresh float array and never mutates the input.\n    \"\"\"\n    if mode not in _RESERVATION_MODES:\n        raise ValueError(f\"reservation mode {mode!r} not in {_RESERVATION_MODES}\")\n    if not (0.0 <= gamma < 1.0):\n        raise ValueError(f\"gamma {gamma!r} must be in [0, 1)\")\n    if not (0.0 <= epsilon < 1.0):\n        raise ValueError(f\"epsilon {epsilon!r} must be in [0, 1)\")\n    S = np.asarray(S_book, float)\n    if S.ndim != 1:\n        raise ValueError(f\"S_book must be one-dimensional, got shape {S.shape}\")\n    if not np.isfinite(S).all():\n        raise ValueError(\"S_book must be finite\")\n    if (S < 0.0).any():\n        raise ValueError(\"S_book must be nonnegative\")\n    region_M = _nonneg_scalar(\"region_M\", region_M)\n    region_T = _nonneg_scalar(\"region_T\", region_T)\n    G0_floor = _nonneg_scalar(\"G0_floor\", G0_floor)\n    d_floor = gamma * G0_floor\n    if mode == \"none\":\n        d = np.zeros(S.shape, float)\n    elif mode == \"uniform\":\n        d = np.full(S.shape, d_floor, float)\n    else:  # \"claims\"\n        alpha = (min(1.0, region_M / region_T) * (1.0 - epsilon)\n                 if region_M > 0.0 and region_T > 0.0 else 0.0)\n        d = alpha * S + d_floor\n    if not np.isfinite(d).all():\n        raise ValueError(\"reservation vector is not finite\")\n    return d\n\n\ndef _nonneg_scalar(name: str, val) -> float:\n    v = np.asarray(val, float)\n    if v.ndim != 0:\n        raise ValueError(f\"{name} must be a scalar, got shape {v.shape}\")\n    x = float(v)\n    if not math.isfinite(x):\n        raise ValueError(f\"{name} must be finite\")\n    if x < 0.0:\n        raise ValueError(f\"{name} must be nonnegative\")\n    return x\n\n\ndef _validate_reservation(reservation, n_reps: int) -> np.ndarray:\n    d = np.asarray(reservation, float)\n    if d.ndim != 1:\n        raise ValueError(f\"reservation must be one-dimensional, got shape {d.shape}\")\n    if d.shape[0] != n_reps:\n        raise ValueError(f\"reservation has {d.shape[0]} entries for {n_reps} reps\")\n    if not np.isfinite(d).all():\n        raise ValueError(\"reservation must be finite\")\n    if (d < 0.0).any():\n        raise ValueError(\"reservation must be nonnegative\")\n    return d\n\n\n@dataclass\nclass Slot:\n    \"\"\"One level-0 slot: a bundle of channels and the share of each state it holds.\"\"\"\n    bundle: tuple[str, ...]\n    y: dict = field(default_factory=dict)      # state -> share in [0, 1]\n    used: bool = True\n\n\n@dataclass\nclass Plan:\n    \"\"\"A channel plan: the slots and the state order the level-0 model used.\n\n    `state_list` is informational here.  Every slot's `y` is keyed by state *name* and is\n    looked up in `cells.state_list`, so the two orders need not agree.\n    \"\"\"\n    slots: list\n    state_list: list = field(default_factory=list)\n\n\n# ------------------------------------------------------------------ index helpers\ndef _rep_rows(cells, reps) -> list:\n    \"\"\"Row indices into `cells.S` for `reps` (all of them, in table order, when None).\"\"\"\n    if reps is None:\n        return list(range(len(cells.reps)))\n    idx = {r: i for i, r in enumerate(cells.reps)}\n    missing = [r for r in reps if r not in idx]\n    if missing:\n        raise ValueError(f\"rep(s) {missing} are not in the cell table\")\n    return [idx[r] for r in reps]\n\n\ndef _channel_cols(cells, bundle) -> list:\n    \"\"\"Column indices into the channel axis for the channels of `bundle`.\"\"\"\n    idx = {c: j for j, c in enumerate(cells.channels)}\n    missing = [c for c in bundle if c not in idx]\n    if missing:\n        raise ValueError(f\"channel(s) {missing} are not in the cell table {cells.channels}\")\n    return [idx[c] for c in bundle]\n\n\ndef _y_vector(cells, y) -> np.ndarray:\n    \"\"\"A slot's state shares as a dense vector over `cells.state_list`.\"\"\"\n    idx = {s: i for i, s in enumerate(cells.state_list)}\n    vec = np.zeros(len(cells.state_list), float)\n    for name, share in y.items():\n        if name not in idx:\n            raise ValueError(f\"state {name!r} is not in the cell table\")\n        vec[idx[name]] = float(share)\n    return vec\n\n\ndef _slot_sums(cells, plan, slot_ids, arr: np.ndarray) -> np.ndarray:\n    \"\"\"`(R, J)`: `arr` summed over each slot's bundle and weighted by its state shares.\"\"\"\n    out = np.zeros((arr.shape[0], len(slot_ids)), float)\n    for col, j in enumerate(slot_ids):\n        slot = plan.slots[j]\n        cols = _channel_cols(cells, slot.bundle)\n        out[:, col] = arr[:, :, cols].sum(2) @ _y_vector(cells, slot.y)\n    return out\n\n\ndef _slot_mass(cells, plan, slot_ids) -> np.ndarray:\n    \"\"\"Opportunity held by each used slot.\"\"\"\n    M = np.asarray(cells.M, float)\n    vals = [M[:, _channel_cols(cells, plan.slots[j].bundle)].sum(1)\n            @ _y_vector(cells, plan.slots[j].y) for j in slot_ids]\n    return np.array(vals, float)\n\n\n# ------------------------------------------------------------------- the utility\ndef state_utilities(cells, reps=None, *, theta: float = 0.40, lam: float = 0.30,\n                    filler_capture: str = \"theta\") -> np.ndarray:\n    \"\"\"`u[i, s, c]` for the reps of `reps` (all of them when None), in that order.\n\n    `T_{s,c}` is a property of the cell, so it sums the book of *every* rep in the table even\n    when `reps` selects a subset: a rep left out of this subproblem still holds its book.\n    \"\"\"\n    c1, c2, c_free = model.coefficients(theta, lam, filler_capture)\n    S_all = np.asarray(cells.S, float)\n    T = S_all.sum(0)                                    # (S, C), over every rep\n    common = (c2 * T + c_free * np.asarray(cells.S_free, float)\n              + lam * np.asarray(cells.M, float))\n    return common[None, :, :] + (c1 - c2) * S_all[_rep_rows(cells, reps)]\n\n\ndef state_gain_matrix(cells, plan, *, reps=None, theta: float = 0.40, lam: float = 0.30,\n                      filler_capture: str = \"theta\",\n                      reservation: np.ndarray | None = None):\n    \"\"\"`(g, reps, slot_ids)` over the *used* slots: `g[i, j]` is rep i's gain from slot j.\n\n    `slot_ids` are indices into `plan.slots`; an unused slot has no column and so can never\n    be matched.  This is `channel.gain_matrix` aggregated: same coefficients, same \"every rep\n    is priced on every slot\" convention (candidacy is a stage-2 restriction, not a stage-1 one).\n\n    With `reservation` (one gain-unit entry per rep, in `R` order) the returned matrix is the\n    centered surplus `g - d_i`, not the raw gain; without it the raw gain is returned.\n    \"\"\"\n    R = list(cells.reps) if reps is None else list(reps)\n    slot_ids = [j for j, slot in enumerate(plan.slots) if slot.used]\n    u = state_utilities(cells, reps, theta=theta, lam=lam, filler_capture=filler_capture)\n    g = _slot_sums(cells, plan, slot_ids, u)\n    if reservation is not None:\n        d = _validate_reservation(reservation, len(R))\n        g = g - d[:, None]\n    return g, R, slot_ids\n\n\n# ---------------------------------------------------------------------- matching\ndef _match_masked(g: np.ndarray, ok: np.ndarray, criterion: str):\n    \"\"\"`channel.match` restricted to the pairs `ok` allows.\n\n    `linear_sum_assignment` matches `min(rows, cols)` cells whatever the costs, so forbidden\n    cells cannot simply be dropped: they are priced above anything a swap of allowed cells can\n    recover, then filtered out of the result.  This is `tools/staff.py`'s `assign`.\n    \"\"\"\n    if criterion == \"nash\":\n        ok = ok & (g > 0.0)        # a zero gain is no staffing, as in `tools/staff.py`\n    if g.size == 0 or not ok.any():\n        return [], 0.0\n    if criterion == \"nash\":\n        with np.errstate(divide=\"ignore\", invalid=\"ignore\"):\n            cost = -np.log(np.where(ok, g, 1.0))\n    elif criterion == \"utilitarian\":\n        cost = -np.where(ok, g, 0.0)\n    else:\n        raise ValueError(f\"criterion {criterion!r} not in ('nash', 'utilitarian')\")\n    lo, hi = float(cost[ok].min()), float(cost[ok].max())\n    pen = hi + (min(g.shape) + 1) * (hi - lo + 1.0)\n    rows, cols = linear_sum_assignment(np.where(ok, cost, pen))\n    pairs = [(int(i), int(j)) for i, j in zip(rows, cols) if ok[i, j]]\n    value = float(sum(math.log(g[i, j]) if criterion == \"nash\" else g[i, j]\n                      for i, j in pairs))\n    return pairs, value\n\n\ndef _check_staffable(g: np.ndarray, ok: np.ndarray, plan, slot_ids) -> None:\n    \"\"\"Every used slot must have at least one candidate with a positive gain.\"\"\"\n    for col, j in enumerate(slot_ids):\n        if not (ok[:, col] & (g[:, col] > 0.0)).any():\n            raise ValueError(\n                f\"slot {j} (bundle {tuple(plan.slots[j].bundle)}) has no candidate rep with a \"\n                f\"positive gain, so it cannot be staffed; drop the slot or widen candidacy\")\n\n\ndef _balance(vals: np.ndarray, target=None) -> dict:\n    \"\"\"`channel.balance_report`'s fields from the slot masses.\"\"\"\n    if vals.size == 0:\n        return dict(k=0)\n    tgt = float(np.mean(vals)) if target is None else float(target)\n    return dict(\n        k=int(vals.size),\n        total=float(vals.sum()),\n        target=tgt,\n        mean=float(vals.mean()),\n        min=float(vals.min()),\n        max=float(vals.max()),\n        spread_rel=float((vals.max() - vals.min()) / vals.mean()) if vals.mean() else 0.0,\n        max_dev_rel=float(np.abs(vals - tgt).max() / tgt) if tgt else 0.0,\n        log_sum=float(np.log(vals).sum()) if (vals > 0).all() else -math.inf,\n    )\n\n\ndef state_stage2(cells, plan, *, reps=None, theta: float = 0.40, lam: float = 0.30,\n                 filler_capture: str = \"theta\", criterion: str = \"nash\",\n                 candidacy: bool = False,\n                 reservation: np.ndarray | None = None,\n                 reservation_floor: float | None = None,\n                 reservation_mode: str = \"none\") -> dict:\n    \"\"\"Staff a channel plan at state grain.  Same keys as `channel.stage2`.\n\n    `districts` holds the used slots' indices into `plan.slots`, so `assignment` and `gains`\n    are keyed by slot id.  With `candidacy=False` this is `channel.match` verbatim, which\n    refuses a non-positive gain anywhere; with `candidacy=True` a rep is only offered the\n    slots where it holds book, and the match is masked rather than the instance changed.\n\n    With `reservation` (one gain-unit entry per rep, in `R` order) the match is run on the raw\n    centered surplus `g - d_i`, never on a clipped matrix: an edge is allowed only when its\n    centered surplus is strictly positive, and a matching that fails to saturate every used\n    slot raises (a Hall shortfall).  `gains` are then the matched centered surpluses and\n    `value`/`value_centered` are computed from them.  `reservation=None` is the legacy `d=0`\n    anchor and keeps the raw gains and legacy value.\n\n    `reservation_floor` supplies the exact common ambient floor `d_floor = gamma * G0_floor`\n    used by the report-only `epsilon_floor = max(1e-6 * d_floor, 1e-12)`.  Without it the\n    floor is recovered as `min(reservation)`, which is exact for uniform mode and for claims\n    mode with at least one zero-claim rep but overstates `d_floor` in claims mode when every\n    rep holds positive book.  The floor is report-only: it never admits or clips an assignment\n    edge.\n    \"\"\"\n    if reservation_mode not in _RESERVATION_MODES:\n        raise ValueError(f\"reservation_mode {reservation_mode!r} not in {_RESERVATION_MODES}\")\n    if reservation_floor is not None:\n        reservation_floor = _nonneg_scalar(\"reservation_floor\", reservation_floor)\n    g, R, slot_ids = state_gain_matrix(cells, plan, reps=reps, theta=theta, lam=lam,\n                                       filler_capture=filler_capture)\n    centered = g\n    d_floor = 0.0\n    mode = \"none\"\n    if reservation is not None:\n        d = _validate_reservation(reservation, len(R))\n        centered = g - d[:, None]\n        if reservation_floor is not None:\n            d_floor = reservation_floor\n        else:\n            # Fallback for callers that supply only the raw reservation vector: exact for\n            # uniform mode and for claims mode with a zero-claim rep, but overstates d_floor\n            # in claims mode when every rep holds positive book.\n            d_floor = float(d.min()) if d.size else 0.0\n        mode = reservation_mode\n    ok = np.ones(centered.shape, bool)\n    if candidacy:\n        book = _slot_sums(cells, plan, slot_ids,\n                          np.asarray(cells.S, float)[_rep_rows(cells, reps)])\n        ok = book > 0.0\n    if reservation is not None:\n        ok = ok & (centered > POSITIVITY_TOL)\n        pairs, value = _match_masked(centered, ok, criterion)\n        short = [slot_ids[j] for j in range(len(slot_ids))\n                 if j not in {j_ for _, j_ in pairs}]\n        if short:\n            raise ValueError(\n                f\"{len(short)} used slot(s) {short} cannot be staffed under the reservation: \"\n                f\"the centered-surplus candidate sets of the used slots have no system of \"\n                f\"distinct representatives (Hall shortfall); lower the reservation or widen \"\n                f\"candidacy\")\n    else:\n        _check_staffable(g, ok, plan, slot_ids)\n        pairs, value = (_match_masked(g, ok, criterion) if candidacy\n                        else channel.match(g, criterion))\n        if candidacy and len(pairs) < len(slot_ids):\n            # `_check_staffable` is per column, so it passes when two slots share their only\n            # candidate: the shortfall is Hall's condition, not an empty column, and only the\n            # matching can see it.  A rep can hold one slot, so the plan is not staffable.\n            short = [slot_ids[j] for j in range(len(slot_ids))\n                     if j not in {j_ for _, j_ in pairs}]\n            raise ValueError(\n                f\"slot(s) {short} cannot be staffed under candidacy: the candidate sets of the \"\n                f\"used slots have no system of distinct representatives; drop a slot or widen \"\n                f\"candidacy\")\n    assign = {slot_ids[j]: R[i] for i, j in pairs}\n    gains = {slot_ids[j]: float(centered[i, j]) for i, j in pairs}\n    matched = {R[i] for i, _ in pairs}\n    return dict(\n        assignment=assign,                       # slot id -> rep\n        gains=gains,                             # slot id -> that rep's (centered) surplus\n        value=value,                             # sum log g (nash) or sum g (utilitarian)\n        value_centered=value,                    # == value: this module's matrix is the center\n        criterion=criterion,\n        reps=R, districts=slot_ids,\n        unmatched_reps=[r for r in R if r not in matched],\n        unstaffed_districts=[j for j in slot_ids if j not in assign],\n        balance=_balance(_slot_mass(cells, plan, slot_ids)),\n        reservation_mode=mode,\n        reservation_grain=\"state\",\n        clipped_edges=0,\n        nonpositive_edges=int((centered <= 0.0).sum()),\n        hall_shortfall=0,\n        epsilon_floor=max(1e-6 * d_floor, 1e-12),\n    )","lineStart":1,"lineEnd":372,"totalLines":372,"truncated":false}},"title":"td/stage2_state.py","time":{"start":1789350537691,"end":1789350537697}},"id":"prt_09d9a68b0001eQIYsd9xX4qLF4","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","messageID":"msg_09d9a3500001I9VIVZzS4ltR3M"}}
{"type":"tool_use","timestamp":1789350538172,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"type":"tool","tool":"read","callID":"call_01_jqKYa2BnvfJ7bdClWlw63499","state":{"status":"completed","input":{"filePath":"/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/tests/test_plan_realise.py","offset":985},"output":"<path>/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/tests/test_plan_realise.py</path>\n<type>file</type>\n<content>\n985:     seed and never anybody's frontier either, so it keeps the power label it started with.\"\"\"\n986:     G = nx.Graph()\n987:     G.add_edges_from([(\"o1\", \"p0\"), (\"p0\", \"p1\"), (\"p1\", \"p2\"), (\"p2\", \"p3\")])\n988:     G.add_node(\"p8\")\n989:     G.add_node(\"p9\")\n990:     zips_s = [\"p0\", \"p1\", \"p2\", \"p3\", \"p8\", \"p9\"]\n991:     power_label = {z: \"other\" for z in zips_s}\n992:     power_label[\"p0\"] = \"D1\"\n993:     power_label[\"p8\"] = \"D1\"                             # preset; growth never reaches it\n994:     M_by_zip = {z: 1.0 for z in zips_s}\n995:     targets = {\"D1\": 2.0, \"other\": 2.0}\n996:     bodies = {\"D1\": {\"o1\"}}\n997:     xy = {\"o1\": (-1.0, 0.0), \"p0\": (0.0, 0.0), \"p1\": (1.0, 0.0), \"p2\": (2.0, 0.0),\n998:           \"p3\": (3.0, 0.0), \"p8\": (5.0, 5.0), \"p9\": (9.0, 9.0)}\n999:     centres = {\"D1\": (0.0, 0.0)}\n1000: \n1001:     labels, dev = cli.contiguous_cut(G, zips_s, power_label, M_by_zip, targets, bodies, centres,\n1002:                                      xy, other=\"other\")\n1003: \n1004:     assert labels[\"p0\"] == labels[\"p1\"] == labels[\"p2\"] == labels[\"p3\"] == \"D1\"\n1005:     assert labels[\"p9\"] == \"other\"                            # the farthest zip, other's seed\n1006:     assert labels[\"p8\"] == \"D1\"                     # its own power label, growth never reached it\n1007:     assert dev[\"D1\"] == (4.0, 2.0) and dev[\"other\"] == (1.0, 2.0)\n\n(End of file - total 1007 lines)\n</content>","metadata":{"preview":"    seed and never anybody's frontier either, so it keeps the power label it started with.\"\"\"\n    G = nx.Graph()\n    G.add_edges_from([(\"o1\", \"p0\"), (\"p0\", \"p1\"), (\"p1\", \"p2\"), (\"p2\", \"p3\")])\n    G.add_node(\"p8\")\n    G.add_node(\"p9\")\n    zips_s = [\"p0\", \"p1\", \"p2\", \"p3\", \"p8\", \"p9\"]\n    power_label = {z: \"other\" for z in zips_s}\n    power_label[\"p0\"] = \"D1\"\n    power_label[\"p8\"] = \"D1\"                             # preset; growth never reaches it\n    M_by_zip = {z: 1.0 for z in zips_s}\n    targets = {\"D1\": 2.0, \"other\": 2.0}\n    bodies = {\"D1\": {\"o1\"}}\n    xy = {\"o1\": (-1.0, 0.0), \"p0\": (0.0, 0.0), \"p1\": (1.0, 0.0), \"p2\": (2.0, 0.0),\n          \"p3\": (3.0, 0.0), \"p8\": (5.0, 5.0), \"p9\": (9.0, 9.0)}\n    centres = {\"D1\": (0.0, 0.0)}\n\n    labels, dev = cli.contiguous_cut(G, zips_s, power_label, M_by_zip, targets, bodies, centres,\n                                     xy, other=\"other\")\n\n    assert labels[\"p0\"] == labels[\"p1\"] == labels[\"p2\"] == labels[\"p3\"] == \"D1\"","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/tests/test_plan_realise.py","text":"    seed and never anybody's frontier either, so it keeps the power label it started with.\"\"\"\n    G = nx.Graph()\n    G.add_edges_from([(\"o1\", \"p0\"), (\"p0\", \"p1\"), (\"p1\", \"p2\"), (\"p2\", \"p3\")])\n    G.add_node(\"p8\")\n    G.add_node(\"p9\")\n    zips_s = [\"p0\", \"p1\", \"p2\", \"p3\", \"p8\", \"p9\"]\n    power_label = {z: \"other\" for z in zips_s}\n    power_label[\"p0\"] = \"D1\"\n    power_label[\"p8\"] = \"D1\"                             # preset; growth never reaches it\n    M_by_zip = {z: 1.0 for z in zips_s}\n    targets = {\"D1\": 2.0, \"other\": 2.0}\n    bodies = {\"D1\": {\"o1\"}}\n    xy = {\"o1\": (-1.0, 0.0), \"p0\": (0.0, 0.0), \"p1\": (1.0, 0.0), \"p2\": (2.0, 0.0),\n          \"p3\": (3.0, 0.0), \"p8\": (5.0, 5.0), \"p9\": (9.0, 9.0)}\n    centres = {\"D1\": (0.0, 0.0)}\n\n    labels, dev = cli.contiguous_cut(G, zips_s, power_label, M_by_zip, targets, bodies, centres,\n                                     xy, other=\"other\")\n\n    assert labels[\"p0\"] == labels[\"p1\"] == labels[\"p2\"] == labels[\"p3\"] == \"D1\"\n    assert labels[\"p9\"] == \"other\"                            # the farthest zip, other's seed\n    assert labels[\"p8\"] == \"D1\"                     # its own power label, growth never reached it\n    assert dev[\"D1\"] == (4.0, 2.0) and dev[\"other\"] == (1.0, 2.0)","lineStart":985,"lineEnd":1007,"totalLines":1007,"truncated":false}},"title":"tests/test_plan_realise.py","time":{"start":1789350538161,"end":1789350538171}},"id":"prt_09d9a6a1b0011a5oi6vfFNlrgp","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","messageID":"msg_09d9a3500001I9VIVZzS4ltR3M"}}
{"type":"step_finish","timestamp":1789350538351,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"id":"prt_09d9a6c6c001xMoVuelkaetYT2","reason":"tool-calls","snapshot":"fb176fc80582c01746f00b4104e610194bba62a1","messageID":"msg_09d9a3500001I9VIVZzS4ltR3M","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","type":"step-finish","tokens":{"total":76424,"input":75158,"output":151,"reasoning":1115,"cache":{"write":0,"read":0}},"cost":0.05211096}}
{"type":"step_start","timestamp":1789350539959,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"id":"prt_09d9a72ad001V6qSo9FzUBDAnb","messageID":"msg_09d9a6cd9001AQ4bWZQYgG8XLp","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","snapshot":"e8445340da8e27a058818acabedf1364dfb0fe35","type":"step-start"}}
{"type":"tool_use","timestamp":1789350682101,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"type":"tool","tool":"read","callID":"call_00_K8RltyXe4aP0PtpPkCbE6838","state":{"status":"completed","input":{"filePath":"/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/td/model.py","offset":60,"limit":140},"output":"<path>/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/td/model.py</path>\n<type>file</type>\n<content>\n60:     return (d[\"rep_a\"], d[\"rep_b\"])\n61: \n62: \n63: def is_contested(G, z) -> bool:\n64:     \"\"\"True iff `z` has two or more candidate owners -- i.e. there is a decision to make.\"\"\"\n65:     return len(candidates(G, z)) >= 2\n66: \n67: \n68: def free_book(G, z) -> float:\n69:     \"\"\"Book at `z` carrying no incumbent rep (the vacancy filler).  0 when absent.\"\"\"\n70:     return float(G.nodes[z].get(FREE, 0.0) or 0.0)\n71: \n72: \n73: def books(G, z) -> dict:\n74:     \"\"\"Per-rep booked production at `z`: {rep -> S}, for native and legacy nodes.\"\"\"\n75:     d = G.nodes[z]\n76:     if BOOK in d:\n77:         return dict(d[BOOK])\n78:     return {d[\"rep_a\"]: float(d[\"A\"]), d[\"rep_b\"]: float(d[\"B\"])}\n79: \n80: \n81: def reps(G, nodes) -> list:\n82:     \"\"\"Every rep appearing as a candidate on `nodes`, in first-appearance order.\n83: \n84:     Deterministic ordering matters: it fixes the row order of the utility matrix and hence\n85:     every downstream index, so runs stay reproducible.\n86:     \"\"\"\n87:     seen, out = set(), []\n88:     for z in nodes:\n89:         for i in candidates(G, z):\n90:             if i not in seen:\n91:                 seen.add(i)\n92:                 out.append(i)\n93:     return out\n94: \n95: \n96: def release_reps(G, released: Iterable[str]):\n97:     \"\"\"Copy of `G` with every rep in `released` gone and their book folded into `S_free`.\n98: \n99:     A released rep leaves the channel, so their book is production with no incumbent -- the\n100:     same thing `S_free` already means.  At every zip the rep's `S_i` moves into `S_free` and\n101:     the rep drops out of `S` and `cand`, so it appears nowhere afterwards and `T_z` for the\n102:     reps who stay shrinks by exactly that much.  A kept rep therefore values a released book\n103:     at `c_free` (`utilities`' `filler_capture`) rather than at `c2`, which is the whole point:\n104:     with `filler_capture=\"full\"` a vacated book is worth `c1`, because nobody is left to pull\n105:     it away.\n106: \n107:     `G` is not mutated; the returned graph's node attributes are fresh objects.\n108:     \"\"\"\n109:     rel = set(released)\n110:     H = G.copy()\n111:     if not rel:\n112:         return H\n113:     for z in H.nodes:\n114:         S = books(G, z)\n115:         moved = sum(float(S.pop(i)) for i in [i for i in S if i in rel])\n116:         d = H.nodes[z]\n117:         d[BOOK] = S\n118:         d[CAND] = tuple(i for i in candidates(G, z) if i not in rel)\n119:         d[FREE] = free_book(G, z) + moved\n120:     return H\n121: \n122: \n123: # ------------------------------------------------------------------- utilities\n124: def coefficients(theta: float, lam: float, filler_capture: str) -> tuple[float, float, float]:\n125:     \"\"\"`(c1, c2, c_free)` of the utility below.  One definition, several call sites.\n126: \n127:     `c1 = 1 - lam` is what an inheriting rep keeps of its own book, `c2 = theta*(1 - lam)`\n128:     what it captures of another rep's, and `c_free` what it makes of unowned book, per\n129:     `filler_capture`.\n130:     \"\"\"\n131:     c1, c2 = 1.0 - lam, theta * (1.0 - lam)\n132:     if filler_capture not in FILLER_CAPTURE:\n133:         raise ValueError(f\"filler_capture {filler_capture!r} not in {FILLER_CAPTURE}\")\n134:     return c1, c2, {\"theta\": c2, \"full\": c1, \"opportunity\": lam}[filler_capture]\n135: \n136: \n137: def utilities(G, nodes, reps_order=None, theta: float = 0.40, lam: float = 0.30,\n138:               filler_capture: str = \"theta\"):\n139:     \"\"\"(U, reps) with `U[k, j]` = u of rep `reps[k]` for `nodes[j]`, 0 where not a candidate.\n140: \n141:     Non-candidates are held at 0 rather than at their notional utility: a rep cannot own a zip\n142:     it is not a candidate for, so the entry is never read by a feasible allocation, and 0\n143:     keeps `gains` a plain masked sum.  Use `candidate_matrix` for the feasibility mask itself.\n144: \n145:     Unowned book (`S_free`, the vacancy filler) is real production with no incumbent, so it\n146:     is never a candidate but every candidate capitalises it::\n147: \n148:         u_i(z) = c1*S_i + c2*(T_z - S_i) + c_free*S_free + lam*M_z\n149: \n150:     `filler_capture` sets `c_free`: \"theta\" -> c2, the same discount as a live rep's book;\n151:     \"full\" -> c1, on the argument that theta < 1 exists because a *departing* rep pulls\n152:     business away and a vacancy has nobody left to pull it; \"opportunity\" -> lam, treating\n153:     the orphaned book as untapped market.  These give materially different allocations --\n154:     NWAY.md 6.7.\n155:     \"\"\"\n156:     nodes = list(nodes)\n157:     R = list(reps_order) if reps_order is not None else reps(G, nodes)\n158:     idx = {i: k for k, i in enumerate(R)}\n159:     c1, c2, c_free = coefficients(theta, lam, filler_capture)\n160:     U = np.zeros((len(R), len(nodes)), float)\n161:     for j, z in enumerate(nodes):\n162:         S = books(G, z)\n163:         T = float(sum(S.values()))\n164:         free = free_book(G, z)\n165:         M = float(G.nodes[z][\"M\"])\n166:         for i in candidates(G, z):\n167:             k = idx.get(i)\n168:             if k is None:                      # rep filtered out of this subproblem\n169:                 continue\n170:             s = float(S.get(i, 0.0))\n171:             U[k, j] = c1 * s + c2 * (T - s) + c_free * free + lam * M\n172:     return U, R\n173: \n174: \n175: def candidate_matrix(G, nodes, reps_order) -> np.ndarray:\n176:     \"\"\"Boolean `C[k, j]`: may rep `reps_order[k]` own `nodes[j]`?\"\"\"\n177:     nodes = list(nodes)\n178:     idx = {i: k for k, i in enumerate(reps_order)}\n179:     C = np.zeros((len(reps_order), len(nodes)), bool)\n180:     for j, z in enumerate(nodes):\n181:         for i in candidates(G, z):\n182:             k = idx.get(i)\n183:             if k is not None:\n184:                 C[k, j] = True\n185:     return C\n186: \n187: \n188: def headroom_violations(G, nodes=None, theta: float = 0.40, tol: float = 0.0) -> list:\n189:     \"\"\"Zips violating `M_z >= max_i (S_i + theta*(T - S_i))`; the N-way headroom condition.\n190: \n191:     Reduces to `M >= max(A + theta*B, B + theta*A)` at two reps (`territory.validate`).\n192:     `tol` is a relative slack on the floor, for callers reading a rounded file format.\n193:     \"\"\"\n194:     bad = []\n195:     for z in (G.nodes() if nodes is None else nodes):\n196:         S = books(G, z)\n197:         free = free_book(G, z)\n198:         T = float(sum(S.values())) + free\n199:         # conservative: the unowned book counts as a possible holder, so a zip whose filler\n\n(Showing lines 60-199 of 357. Use offset=200 to continue.)\n</content>","metadata":{"preview":"    return (d[\"rep_a\"], d[\"rep_b\"])\n\n\ndef is_contested(G, z) -> bool:\n    \"\"\"True iff `z` has two or more candidate owners -- i.e. there is a decision to make.\"\"\"\n    return len(candidates(G, z)) >= 2\n\n\ndef free_book(G, z) -> float:\n    \"\"\"Book at `z` carrying no incumbent rep (the vacancy filler).  0 when absent.\"\"\"\n    return float(G.nodes[z].get(FREE, 0.0) or 0.0)\n\n\ndef books(G, z) -> dict:\n    \"\"\"Per-rep booked production at `z`: {rep -> S}, for native and legacy nodes.\"\"\"\n    d = G.nodes[z]\n    if BOOK in d:\n        return dict(d[BOOK])\n    return {d[\"rep_a\"]: float(d[\"A\"]), d[\"rep_b\"]: float(d[\"B\"])}\n","truncated":true,"loaded":[],"display":{"type":"file","path":"/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/td/model.py","text":"    return (d[\"rep_a\"], d[\"rep_b\"])\n\n\ndef is_contested(G, z) -> bool:\n    \"\"\"True iff `z` has two or more candidate owners -- i.e. there is a decision to make.\"\"\"\n    return len(candidates(G, z)) >= 2\n\n\ndef free_book(G, z) -> float:\n    \"\"\"Book at `z` carrying no incumbent rep (the vacancy filler).  0 when absent.\"\"\"\n    return float(G.nodes[z].get(FREE, 0.0) or 0.0)\n\n\ndef books(G, z) -> dict:\n    \"\"\"Per-rep booked production at `z`: {rep -> S}, for native and legacy nodes.\"\"\"\n    d = G.nodes[z]\n    if BOOK in d:\n        return dict(d[BOOK])\n    return {d[\"rep_a\"]: float(d[\"A\"]), d[\"rep_b\"]: float(d[\"B\"])}\n\n\ndef reps(G, nodes) -> list:\n    \"\"\"Every rep appearing as a candidate on `nodes`, in first-appearance order.\n\n    Deterministic ordering matters: it fixes the row order of the utility matrix and hence\n    every downstream index, so runs stay reproducible.\n    \"\"\"\n    seen, out = set(), []\n    for z in nodes:\n        for i in candidates(G, z):\n            if i not in seen:\n                seen.add(i)\n                out.append(i)\n    return out\n\n\ndef release_reps(G, released: Iterable[str]):\n    \"\"\"Copy of `G` with every rep in `released` gone and their book folded into `S_free`.\n\n    A released rep leaves the channel, so their book is production with no incumbent -- the\n    same thing `S_free` already means.  At every zip the rep's `S_i` moves into `S_free` and\n    the rep drops out of `S` and `cand`, so it appears nowhere afterwards and `T_z` for the\n    reps who stay shrinks by exactly that much.  A kept rep therefore values a released book\n    at `c_free` (`utilities`' `filler_capture`) rather than at `c2`, which is the whole point:\n    with `filler_capture=\"full\"` a vacated book is worth `c1`, because nobody is left to pull\n    it away.\n\n    `G` is not mutated; the returned graph's node attributes are fresh objects.\n    \"\"\"\n    rel = set(released)\n    H = G.copy()\n    if not rel:\n        return H\n    for z in H.nodes:\n        S = books(G, z)\n        moved = sum(float(S.pop(i)) for i in [i for i in S if i in rel])\n        d = H.nodes[z]\n        d[BOOK] = S\n        d[CAND] = tuple(i for i in candidates(G, z) if i not in rel)\n        d[FREE] = free_book(G, z) + moved\n    return H\n\n\n# ------------------------------------------------------------------- utilities\ndef coefficients(theta: float, lam: float, filler_capture: str) -> tuple[float, float, float]:\n    \"\"\"`(c1, c2, c_free)` of the utility below.  One definition, several call sites.\n\n    `c1 = 1 - lam` is what an inheriting rep keeps of its own book, `c2 = theta*(1 - lam)`\n    what it captures of another rep's, and `c_free` what it makes of unowned book, per\n    `filler_capture`.\n    \"\"\"\n    c1, c2 = 1.0 - lam, theta * (1.0 - lam)\n    if filler_capture not in FILLER_CAPTURE:\n        raise ValueError(f\"filler_capture {filler_capture!r} not in {FILLER_CAPTURE}\")\n    return c1, c2, {\"theta\": c2, \"full\": c1, \"opportunity\": lam}[filler_capture]\n\n\ndef utilities(G, nodes, reps_order=None, theta: float = 0.40, lam: float = 0.30,\n              filler_capture: str = \"theta\"):\n    \"\"\"(U, reps) with `U[k, j]` = u of rep `reps[k]` for `nodes[j]`, 0 where not a candidate.\n\n    Non-candidates are held at 0 rather than at their notional utility: a rep cannot own a zip\n    it is not a candidate for, so the entry is never read by a feasible allocation, and 0\n    keeps `gains` a plain masked sum.  Use `candidate_matrix` for the feasibility mask itself.\n\n    Unowned book (`S_free`, the vacancy filler) is real production with no incumbent, so it\n    is never a candidate but every candidate capitalises it::\n\n        u_i(z) = c1*S_i + c2*(T_z - S_i) + c_free*S_free + lam*M_z\n\n    `filler_capture` sets `c_free`: \"theta\" -> c2, the same discount as a live rep's book;\n    \"full\" -> c1, on the argument that theta < 1 exists because a *departing* rep pulls\n    business away and a vacancy has nobody left to pull it; \"opportunity\" -> lam, treating\n    the orphaned book as untapped market.  These give materially different allocations --\n    NWAY.md 6.7.\n    \"\"\"\n    nodes = list(nodes)\n    R = list(reps_order) if reps_order is not None else reps(G, nodes)\n    idx = {i: k for k, i in enumerate(R)}\n    c1, c2, c_free = coefficients(theta, lam, filler_capture)\n    U = np.zeros((len(R), len(nodes)), float)\n    for j, z in enumerate(nodes):\n        S = books(G, z)\n        T = float(sum(S.values()))\n        free = free_book(G, z)\n        M = float(G.nodes[z][\"M\"])\n        for i in candidates(G, z):\n            k = idx.get(i)\n            if k is None:                      # rep filtered out of this subproblem\n                continue\n            s = float(S.get(i, 0.0))\n            U[k, j] = c1 * s + c2 * (T - s) + c_free * free + lam * M\n    return U, R\n\n\ndef candidate_matrix(G, nodes, reps_order) -> np.ndarray:\n    \"\"\"Boolean `C[k, j]`: may rep `reps_order[k]` own `nodes[j]`?\"\"\"\n    nodes = list(nodes)\n    idx = {i: k for k, i in enumerate(reps_order)}\n    C = np.zeros((len(reps_order), len(nodes)), bool)\n    for j, z in enumerate(nodes):\n        for i in candidates(G, z):\n            k = idx.get(i)\n            if k is not None:\n                C[k, j] = True\n    return C\n\n\ndef headroom_violations(G, nodes=None, theta: float = 0.40, tol: float = 0.0) -> list:\n    \"\"\"Zips violating `M_z >= max_i (S_i + theta*(T - S_i))`; the N-way headroom condition.\n\n    Reduces to `M >= max(A + theta*B, B + theta*A)` at two reps (`territory.validate`).\n    `tol` is a relative slack on the floor, for callers reading a rounded file format.\n    \"\"\"\n    bad = []\n    for z in (G.nodes() if nodes is None else nodes):\n        S = books(G, z)\n        free = free_book(G, z)\n        T = float(sum(S.values())) + free\n        # conservative: the unowned book counts as a possible holder, so a zip whose filler","lineStart":60,"lineEnd":199,"totalLines":357,"truncated":true}},"title":"td/model.py","time":{"start":1789350682094,"end":1789350682099}},"id":"prt_09d9c9c3c00166Zlh6m7OKXMEq","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","messageID":"msg_09d9a6cd9001AQ4bWZQYgG8XLp"}}
{"type":"step_finish","timestamp":1789350682377,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"id":"prt_09d9c9f07001XRh5EMNrGUcqrL","reason":"tool-calls","snapshot":"35656df77b2169b835f2884714b3740029587e81","messageID":"msg_09d9a6cd9001AQ4bWZQYgG8XLp","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","type":"step-finish","tokens":{"total":96523,"input":6286,"output":103,"reasoning":13718,"cache":{"write":0,"read":76416}},"cost":0.033195492}}
{"type":"step_start","timestamp":1789350684939,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"id":"prt_09d9ca909001rux1pB8nfPqI8C","messageID":"msg_09d9c9f73001yczi8sbUoLuTBU","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","snapshot":"9ae28efcd9fcf2122355aedbcc7a39dcf6870622","type":"step-start"}}
{"type":"tool_use","timestamp":1789350687904,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"type":"tool","tool":"read","callID":"call_00_WSaDCDCwGucysXzU2BfS1262","state":{"status":"completed","input":{"filePath":"/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/tests/run_all.py"},"output":"<path>/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/tests/run_all.py</path>\n<type>file</type>\n<content>\n1: \"\"\"\n2: run_all.py -- plain test runner for tests (pytest-compatible layout).\n3: \n4: Discovers every `test_*.py` in this directory, imports it, and runs every callable\n5: whose name starts with `test_`.  A module that sets `SLOW = True` is skipped unless\n6: the environment variable `TD_SLOW=1` is set (the zip50 anchor takes ~2 min).\n7: \n8: Run from the repo root:\n9:     .venv/bin/python3 tests/run_all.py            # fast tests\n10:     TD_SLOW=1 .venv/bin/python3 tests/run_all.py  # + anchors\n11:     .venv/bin/python3 tests/run_all.py -k base    # name filter\n12: \n13: Exit status is nonzero if any test fails.  Also works under `pytest tests`.\n14: \"\"\"\n15: from __future__ import annotations\n16: import importlib.util, os, sys, time, traceback\n17: \n18: HERE = os.path.dirname(os.path.abspath(__file__))\n19: ROOT = os.path.abspath(os.path.join(HERE, \"..\"))\n20: if ROOT not in sys.path:            # so `import td` works however this is invoked\n21:     sys.path.insert(0, ROOT)\n22: \n23: \n24: def _load(path):\n25:     name = \"tests.\" + os.path.splitext(os.path.basename(path))[0]\n26:     spec = importlib.util.spec_from_file_location(name, path)\n27:     mod = importlib.util.module_from_spec(spec)\n28:     spec.loader.exec_module(mod)\n29:     return mod\n30: \n31: \n32: def main(argv=None):\n33:     argv = list(sys.argv[1:] if argv is None else argv)\n34:     pattern = None\n35:     if \"-k\" in argv:\n36:         pattern = argv[argv.index(\"-k\") + 1]\n37:     slow = os.environ.get(\"TD_SLOW\", \"0\") not in (\"\", \"0\")\n38:     os.chdir(ROOT)                      # every script assumes the repo root\n39:     files = sorted(f for f in os.listdir(HERE) if f.startswith(\"test_\") and f.endswith(\".py\"))\n40:     n_pass = n_fail = n_skip = 0\n41:     failures = []\n42:     for f in files:\n43:         mod = _load(os.path.join(HERE, f))\n44:         if getattr(mod, \"SLOW\", False) and not slow:\n45:             print(f\"SKIP  {f}  (set TD_SLOW=1)\")\n46:             n_skip += 1\n47:             continue\n48:         tests = [(k, v) for k, v in vars(mod).items()\n49:                  if k.startswith(\"test_\") and callable(v)]\n50:         for name, fn in tests:\n51:             full = f\"{f}::{name}\"\n52:             if pattern and pattern not in full:\n53:                 n_skip += 1\n54:                 continue\n55:             t0 = time.time()\n56:             try:\n57:                 fn()\n58:                 n_pass += 1\n59:                 print(f\"PASS  {full}  ({time.time()-t0:.1f}s)\")\n60:             except Exception:\n61:                 n_fail += 1\n62:                 failures.append(full)\n63:                 print(f\"FAIL  {full}  ({time.time()-t0:.1f}s)\")\n64:                 traceback.print_exc()\n65:     print(f\"\\n{n_pass} passed, {n_fail} failed, {n_skip} skipped\")\n66:     if failures:\n67:         print(\"failures:\", *failures, sep=\"\\n  \")\n68:     return 1 if failures else 0\n69: \n70: \n71: if __name__ == \"__main__\":\n72:     sys.exit(main())\n\n(End of file - total 72 lines)\n</content>","metadata":{"preview":"\"\"\"\nrun_all.py -- plain test runner for tests (pytest-compatible layout).\n\nDiscovers every `test_*.py` in this directory, imports it, and runs every callable\nwhose name starts with `test_`.  A module that sets `SLOW = True` is skipped unless\nthe environment variable `TD_SLOW=1` is set (the zip50 anchor takes ~2 min).\n\nRun from the repo root:\n    .venv/bin/python3 tests/run_all.py            # fast tests\n    TD_SLOW=1 .venv/bin/python3 tests/run_all.py  # + anchors\n    .venv/bin/python3 tests/run_all.py -k base    # name filter\n\nExit status is nonzero if any test fails.  Also works under `pytest tests`.\n\"\"\"\nfrom __future__ import annotations\nimport importlib.util, os, sys, time, traceback\n\nHERE = os.path.dirname(os.path.abspath(__file__))\nROOT = os.path.abspath(os.path.join(HERE, \"..\"))\nif ROOT not in sys.path:            # so `import td` works however this is invoked","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/tests/run_all.py","text":"\"\"\"\nrun_all.py -- plain test runner for tests (pytest-compatible layout).\n\nDiscovers every `test_*.py` in this directory, imports it, and runs every callable\nwhose name starts with `test_`.  A module that sets `SLOW = True` is skipped unless\nthe environment variable `TD_SLOW=1` is set (the zip50 anchor takes ~2 min).\n\nRun from the repo root:\n    .venv/bin/python3 tests/run_all.py            # fast tests\n    TD_SLOW=1 .venv/bin/python3 tests/run_all.py  # + anchors\n    .venv/bin/python3 tests/run_all.py -k base    # name filter\n\nExit status is nonzero if any test fails.  Also works under `pytest tests`.\n\"\"\"\nfrom __future__ import annotations\nimport importlib.util, os, sys, time, traceback\n\nHERE = os.path.dirname(os.path.abspath(__file__))\nROOT = os.path.abspath(os.path.join(HERE, \"..\"))\nif ROOT not in sys.path:            # so `import td` works however this is invoked\n    sys.path.insert(0, ROOT)\n\n\ndef _load(path):\n    name = \"tests.\" + os.path.splitext(os.path.basename(path))[0]\n    spec = importlib.util.spec_from_file_location(name, path)\n    mod = importlib.util.module_from_spec(spec)\n    spec.loader.exec_module(mod)\n    return mod\n\n\ndef main(argv=None):\n    argv = list(sys.argv[1:] if argv is None else argv)\n    pattern = None\n    if \"-k\" in argv:\n        pattern = argv[argv.index(\"-k\") + 1]\n    slow = os.environ.get(\"TD_SLOW\", \"0\") not in (\"\", \"0\")\n    os.chdir(ROOT)                      # every script assumes the repo root\n    files = sorted(f for f in os.listdir(HERE) if f.startswith(\"test_\") and f.endswith(\".py\"))\n    n_pass = n_fail = n_skip = 0\n    failures = []\n    for f in files:\n        mod = _load(os.path.join(HERE, f))\n        if getattr(mod, \"SLOW\", False) and not slow:\n            print(f\"SKIP  {f}  (set TD_SLOW=1)\")\n            n_skip += 1\n            continue\n        tests = [(k, v) for k, v in vars(mod).items()\n                 if k.startswith(\"test_\") and callable(v)]\n        for name, fn in tests:\n            full = f\"{f}::{name}\"\n            if pattern and pattern not in full:\n                n_skip += 1\n                continue\n            t0 = time.time()\n            try:\n                fn()\n                n_pass += 1\n                print(f\"PASS  {full}  ({time.time()-t0:.1f}s)\")\n            except Exception:\n                n_fail += 1\n                failures.append(full)\n                print(f\"FAIL  {full}  ({time.time()-t0:.1f}s)\")\n                traceback.print_exc()\n    print(f\"\\n{n_pass} passed, {n_fail} failed, {n_skip} skipped\")\n    if failures:\n        print(\"failures:\", *failures, sep=\"\\n  \")\n    return 1 if failures else 0\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())","lineStart":1,"lineEnd":72,"totalLines":72,"truncated":false}},"title":"tests/run_all.py","time":{"start":1789350687899,"end":1789350687903}},"id":"prt_09d9cb36e001k2UKNokJqxS4cA","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","messageID":"msg_09d9c9f73001yczi8sbUoLuTBU"}}
{"type":"step_finish","timestamp":1789350688087,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"id":"prt_09d9cb551001DJK9DEQwkJLaZX","reason":"tool-calls","snapshot":"b0ea0817d57d530d1cf081d23a180aff4c728a7d","messageID":"msg_09d9c9f73001yczi8sbUoLuTBU","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","type":"step-finish","tokens":{"total":98818,"input":2076,"output":73,"reasoning":157,"cache":{"write":0,"read":96512}},"cost":0.003948824}}
{"type":"step_start","timestamp":1789350689912,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"id":"prt_09d9cbc77001UdBJnlGz67cCdI","messageID":"msg_09d9cb5bf001oOm9iG2J1Y8M97","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","snapshot":"18395222645fc035182c11a9f6a965c6ca01d52d","type":"step-start"}}
{"type":"tool_use","timestamp":1789350694350,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"type":"tool","tool":"bash","callID":"call_01_PJ0oMT9aqIefkJpONLeS0899","state":{"status":"completed","input":{"command":"cd /Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review && git status --short --branch && git log --oneline -5"},"output":"## worktree-agy-math-review...origin/worktree-agy-math-review\n M td/stage2_state.py\n M tests/test_full_plan_cli.py\n M tests/test_stage2_state.py\n M tools/full_plan.py\n?? agy-job/\n?? tests/test_fefx.py\n?? tools/measure/fefx.py\n152f4ff agy-math-review: require yolo mode for OpenCode sessions\nb164e82 agy-math-review: document ad hoc OpenCode orchestration\n13ad7dc docs: mathematical review, FEFx axiomatic foundations, and tri-agent implementation plan\nd36dc40 scenario_export.py, export --rep-map: newline line endings, so a reader that breaks on carriage returns shows no blank rows\ndbd7fbc Scenario datasets: scenario_export.py joins realised runs onto the source rows; export --rep-map writes the surrogate rep id map\n","metadata":{"output":"## worktree-agy-math-review...origin/worktree-agy-math-review\n M td/stage2_state.py\n M tests/test_full_plan_cli.py\n M tests/test_stage2_state.py\n M tools/full_plan.py\n?? agy-job/\n?? tests/test_fefx.py\n?? tools/measure/fefx.py\n152f4ff agy-math-review: require yolo mode for OpenCode sessions\nb164e82 agy-math-review: document ad hoc OpenCode orchestration\n13ad7dc docs: mathematical review, FEFx axiomatic foundations, and tri-agent implementation plan\nd36dc40 scenario_export.py, export --rep-map: newline line endings, so a reader that breaks on carriage returns shows no blank rows\ndbd7fbc Scenario datasets: scenario_export.py joins realised runs onto the source rows; export --rep-map writes the surrogate rep id map\n","exit":0,"truncated":false},"title":"cd /Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review && git status --short --branch && git log --oneline -5","time":{"start":1789350694298,"end":1789350694349}},"id":"prt_09d9ccc49001aaU2h0o6Hzsi1K","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","messageID":"msg_09d9cb5bf001oOm9iG2J1Y8M97"}}
{"type":"tool_use","timestamp":1789350694740,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"type":"tool","tool":"bash","callID":"call_00_LRcAKinNV20Hl5kOR3S52600","state":{"status":"completed","input":{"command":"cd /Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review && /Users/Shared/sv-ntlee/repos/td/.venv/bin/python3 -u tests/run_all.py -k test_plan_realise 2>&1 | tail -n 40"},"output":"N_WH: 0 zip(s) assigned, residual mass 1.11127\nN_FI: 2 zip(s) assigned, residual mass 0\nWH: 0 zip(s) assigned, residual mass 0.959024\nFI: 2 zip(s) assigned, residual mass 0\nwrote /var/folders/5d/mzf0tpm55sn84f1y1kz7kpdr0000ks/T/tmp.VovKv3XBDt/tmplxa8hl54\nPASS  test_plan_realise.py::test_a_gap_zip_with_no_adjacent_candidate_falls_back_to_the_largest_holder  (0.0s)\nPASS  test_plan_realise.py::test_a_detached_piece_that_one_move_fixes_is_moved  (0.0s)\nPASS  test_plan_realise.py::test_a_piece_with_no_admissible_neighbour_stays_and_is_reported  (0.0s)\nPASS  test_plan_realise.py::test_the_repair_guards_each_bundle_with_the_plans_own_band  (0.0s)\nPASS  test_plan_realise.py::test_a_move_that_would_push_a_district_out_of_the_band_is_refused  (0.0s)\nPASS  test_plan_realise.py::test_the_band_slack_lets_a_small_piece_cross_into_a_district_at_the_top  (0.0s)\nPASS  test_plan_realise.py::test_a_whole_state_district_is_never_opened_by_the_repair  (0.0s)\nPASS  test_plan_realise.py::test_an_off_plan_zip_is_moved_to_a_district_the_plan_admits  (0.0s)\nPASS  test_plan_realise.py::test_a_detached_piece_bridges_to_its_only_neighbour_with_a_swap_back  (0.0s)\nPASS  test_plan_realise.py::test_a_bridge_that_would_raise_the_pair_total_is_refused  (0.0s)\nPASS  test_plan_realise.py::test_a_district_holding_nothing_but_off_plan_zips_keeps_them  (0.0s)\nplan: 2 used slot(s) over 1 bundle(s) ['N'], 2 staffed, band unset\nN: 2 district(s), 2 split state(s), 1 zip(s) other; graph cell_rook 6 zip(s) 5 edge(s) 1 component(s), 2 cross-state; pieces 2 -> 2, 0 zip(s) moved, 0 bridge(s) (0 zip(s) swapped), 0 off-plan (mass 0), 0 handed off, 0 district(s) still split (0.0s)\ndistricts=2 staffed=2 unstaffed=0 disconnected=0 moved=0 band_violations=0\nN_WH: 5 zip(s) assigned, residual mass 0.295951\nN_FI: 5 zip(s) assigned, residual mass 0.704049\nWH: 0 zip(s) assigned, residual mass 1.84776\nFI: 0 zip(s) assigned, residual mass 1.72474\nwrote /var/folders/5d/mzf0tpm55sn84f1y1kz7kpdr0000ks/T/tmp.VovKv3XBDt/tmprrquel6s\nPASS  test_plan_realise.py::test_split_cut_contiguous_recuts_named_bundles_on_the_cell_graph  (0.0s)\nplan: 2 used slot(s) over 1 bundle(s) ['N'], 2 staffed, band unset\nN: 2 district(s), 2 split state(s), 1 zip(s) other; graph cell_rook 6 zip(s) 5 edge(s) 1 component(s), 2 cross-state; pieces 2 -> 2, 0 zip(s) moved, 0 bridge(s) (0 zip(s) swapped), 0 off-plan (mass 0), 0 handed off, 0 district(s) still split (0.0s)\ndistricts=2 staffed=2 unstaffed=0 disconnected=0 moved=0 band_violations=0\nN_WH: 5 zip(s) assigned, residual mass 0.295951\nN_FI: 5 zip(s) assigned, residual mass 0.704049\nWH: 0 zip(s) assigned, residual mass 1.84776\nFI: 0 zip(s) assigned, residual mass 1.72474\nwrote /var/folders/5d/mzf0tpm55sn84f1y1kz7kpdr0000ks/T/tmp.VovKv3XBDt/tmph3spfzds\nPASS  test_plan_realise.py::test_split_cut_contiguous_defaults_to_the_wh_bundle_and_leaves_others_on_power  (0.0s)\nPASS  test_plan_realise.py::test_contiguous_cut_seeds_at_the_east_border_even_when_the_power_share_is_west  (0.0s)\nPASS  test_plan_realise.py::test_contiguous_cut_without_a_border_keeps_the_centre_seed  (0.0s)\nPASS  test_plan_realise.py::test_contiguous_cut_reunites_a_detached_zip_and_seeds_a_wholly_inside_district_at_its_centre  (0.0s)\nPASS  test_plan_realise.py::test_contiguous_cut_seeds_other_farthest_from_every_real_seed_and_leaves_unreached_zips_alone  (0.0s)\n\n30 passed, 0 failed, 833 skipped\n","metadata":{"output":"N_WH: 0 zip(s) assigned, residual mass 1.11127\nN_FI: 2 zip(s) assigned, residual mass 0\nWH: 0 zip(s) assigned, residual mass 0.959024\nFI: 2 zip(s) assigned, residual mass 0\nwrote /var/folders/5d/mzf0tpm55sn84f1y1kz7kpdr0000ks/T/tmp.VovKv3XBDt/tmplxa8hl54\nPASS  test_plan_realise.py::test_a_gap_zip_with_no_adjacent_candidate_falls_back_to_the_largest_holder  (0.0s)\nPASS  test_plan_realise.py::test_a_detached_piece_that_one_move_fixes_is_moved  (0.0s)\nPASS  test_plan_realise.py::test_a_piece_with_no_admissible_neighbour_stays_and_is_reported  (0.0s)\nPASS  test_plan_realise.py::test_the_repair_guards_each_bundle_with_the_plans_own_band  (0.0s)\nPASS  test_plan_realise.py::test_a_move_that_would_push_a_district_out_of_the_band_is_refused  (0.0s)\nPASS  test_plan_realise.py::test_the_band_slack_lets_a_small_piece_cross_into_a_district_at_the_top  (0.0s)\nPASS  test_plan_realise.py::test_a_whole_state_district_is_never_opened_by_the_repair  (0.0s)\nPASS  test_plan_realise.py::test_an_off_plan_zip_is_moved_to_a_district_the_plan_admits  (0.0s)\nPASS  test_plan_realise.py::test_a_detached_piece_bridges_to_its_only_neighbour_with_a_swap_back  (0.0s)\nPASS  test_plan_realise.py::test_a_bridge_that_would_raise_the_pair_total_is_refused  (0.0s)\nPASS  test_plan_realise.py::test_a_district_holding_nothing_but_off_plan_zips_keeps_them  (0.0s)\nplan: 2 used slot(s) over 1 bundle(s) ['N'], 2 staffed, band unset\nN: 2 district(s), 2 split state(s), 1 zip(s) other; graph cell_rook 6 zip(s) 5 edge(s) 1 component(s), 2 cross-state; pieces 2 -> 2, 0 zip(s) moved, 0 bridge(s) (0 zip(s) swapped), 0 off-plan (mass 0), 0 handed off, 0 district(s) still split (0.0s)\ndistricts=2 staffed=2 unstaffed=0 disconnected=0 moved=0 band_violations=0\nN_WH: 5 zip(s) assigned, residual mass 0.295951\nN_FI: 5 zip(s) assigned, residual mass 0.704049\nWH: 0 zip(s) assigned, residual mass 1.84776\nFI: 0 zip(s) assigned, residual mass 1.72474\nwrote /var/folders/5d/mzf0tpm55sn84f1y1kz7kpdr0000ks/T/tmp.VovKv3XBDt/tmprrquel6s\nPASS  test_plan_realise.py::test_split_cut_contiguous_recuts_named_bundles_on_the_cell_graph  (0.0s)\nplan: 2 used slot(s) over 1 bundle(s) ['N'], 2 staffed, band unset\nN: 2 district(s), 2 split state(s), 1 zip(s) other; graph cell_rook 6 zip(s) 5 edge(s) 1 component(s), 2 cross-state; pieces 2 -> 2, 0 zip(s) moved, 0 bridge(s) (0 zip(s) swapped), 0 off-plan (mass 0), 0 handed off, 0 district(s) still split (0.0s)\ndistricts=2 staffed=2 unstaffed=0 disconnected=0 moved=0 band_violations=0\nN_WH: 5 zip(s) assigned, residual mass 0.295951\nN_FI: 5 zip(s) assigned, residual mass 0.704049\nWH: 0 zip(s) assigned, residual mass 1.84776\nFI: 0 zip(s) assigned, residual mass 1.72474\nwrote /var/folders/5d/mzf0tpm55sn84f1y1kz7kpdr0000ks/T/tmp.VovKv3XBDt/tmph3spfzds\nPASS  test_plan_realise.py::test_split_cut_contiguous_defaults_to_the_wh_bundle_and_leaves_others_on_power  (0.0s)\nPASS  test_plan_realise.py::test_contiguous_cut_seeds_at_the_east_border_even_when_the_power_share_is_west  (0.0s)\nPASS  test_plan_realise.py::test_contiguous_cut_without_a_border_keeps_the_centre_seed  (0.0s)\nPASS  test_plan_realise.py::test_contiguous_cut_reunites_a_detached_zip_and_seeds_a_wholly_inside_district_at_its_centre  (0.0s)\nPASS  test_plan_realise.py::test_contiguous_cut_seeds_other_farthest_from_every_real_seed_and_leaves_unreached_zips_alone  (0.0s)\n\n30 passed, 0 failed, 833 skipped\n","exit":0,"truncated":false},"title":"cd /Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review && /Users/Shared/sv-ntlee/repos/td/.venv/bin/python3 -u tests/run_all.py -k test_plan_realise 2>&1 | tail -n 40","time":{"start":1789350693933,"end":1789350694738}},"id":"prt_09d9cc9ec001ni2lk7YelNKj7h","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","messageID":"msg_09d9cb5bf001oOm9iG2J1Y8M97"}}
{"type":"step_finish","timestamp":1789350694843,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"id":"prt_09d9ccfb90011FC5dHyQYo9dU0","reason":"tool-calls","snapshot":"3c3d0b2001a5cd68b5bf89d43d5f48a38eeff131","messageID":"msg_09d9cb5bf001oOm9iG2J1Y8M97","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","type":"step-finish","tokens":{"total":100172,"input":974,"output":179,"reasoning":203,"cache":{"write":0,"read":98816}},"cost":0.003573152}}
{"type":"step_start","timestamp":1789350696680,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"id":"prt_09d9cd6e5001HKkxh2Ktf4nPdc","messageID":"msg_09d9cd028001yqIlIiXQ08u5x5","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","snapshot":"f34df72fe399b0ca9f0659b75227a05a47bffd7c","type":"step-start"}}
{"type":"tool_use","timestamp":1789350765519,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"type":"tool","tool":"read","callID":"call_00_tDNVc3O4WsJTSPic9vzg8263","state":{"status":"completed","input":{"filePath":"/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/tools/plan_realise.py","offset":1000},"output":"<path>/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/tools/plan_realise.py</path>\n<type>file</type>\n<content>\n1000:     A named bundle leaves its name there and a catch-all bundle its channel list, so this is\n1001:     the one reader that covers both without importing the driver that made them.\n1002:     \"\"\"\n1003:     named = proj.meta.get(\"bundle\")\n1004:     if isinstance(named, str):\n1005:         named = channels.BUNDLES.get(named)\n1006:     chans = tuple(named or ())\n1007:     unknown = [c for c in chans if c not in channels.CHANNELS]\n1008:     if not chans or unknown:\n1009:         raise ValueError(f\"bundle {bundle!r}: the projection's meta['bundle'] is \"\n1010:                          f\"{proj.meta.get('bundle')!r}, not a channel list this run can key \"\n1011:                          f\"a cell table by\")\n1012:     return chans\n1013: \n1014: \n1015: def _overlaps(by_bundle: dict, chans_of: dict) -> list:\n1016:     \"\"\"(state, channel) pairs two used bundles both take a share of.  Level 0's cover row is\n1017:     `<= 1` per cell summed over slots, so this is a plan the projections cannot express.\"\"\"\n1018:     seen: dict[tuple, set] = {}\n1019:     for bundle, recs in by_bundle.items():\n1020:         for c in chans_of.get(bundle, ()):\n1021:             for _, rec in recs:\n1022:                 for st in rec[\"y\"]:\n1023:                     seen.setdefault((st, c), set()).add(bundle)\n1024:     return [dict(state=st, channel=c, bundles=sorted(bs))\n1025:             for (st, c), bs in sorted(seen.items()) if len(bs) > 1]\n1026: \n1027: \n1028: def band_of(params: dict, bundle: str, default):\n1029:     \"\"\"The band the plan held this bundle's districts to.  Under `--band-mode per-bundle` the\n1030:     driver records one band per bundle (`params[\"bands\"][bundle]`, tau_B = M_B / k_B), and that\n1031:     is the band the repair must guard; `default` (the file-level `L`, `U`) is the band of a run\n1032:     without per-bundle bands.\"\"\"\n1033:     rec = (params.get(\"bands\") or {}).get(bundle) or {}\n1034:     if rec.get(\"L\") is not None and rec.get(\"U\") is not None:\n1035:         return (float(rec[\"L\"]), float(rec[\"U\"]))\n1036:     return default\n1037: \n1038: \n1039: def _n_file_channels(bundle: str) -> int:\n1040:     \"\"\"How many of the three file channels a bundle carries: WHFI_PLUS 3; WHFI, WH_PLUS and\n1041:     FI_PLUS 2; WH, FI and N 1.  N's two fine labels are one file channel, so it does not\n1042:     outrank a pure bundle.\"\"\"\n1043:     return len({FILE_OF[c] for c in channels.BUNDLES.get(bundle, ())})\n1044: \n1045: \n1046: def _claim_cells(d, cell_of: dict, book: dict, name: str, bundle: str, rep: str, zp: str,\n1047:                  chans) -> tuple:\n1048:     \"\"\"Claim `zp`'s cells on `chans` not already in `cell_of`, crediting `name`'s mass and, when\n1049:     `rep` holds book there, `book[name]`.  Returns `(mass_added, claimed)`; `claimed` is\n1050:     `False` when every channel in `chans` was already claimed, so the caller counts no zip.\n1051: \n1052:     The one place a cell actually changes hands, shared by the per-district accounting loop in\n1053:     `_main` and `_handoff_overlap_pieces`.\n1054:     \"\"\"\n1055:     kept = [c for c in chans if (zp, c) not in cell_of]\n1056:     if not kept:\n1057:         return 0.0, False\n1058:     a = d.G.nodes[zp]\n1059:     M_c = dict(a.get(\"M_c\") or {})\n1060:     mass = sum(float(M_c.get(c, 0.0)) for c in kept)\n1061:     for c in kept:\n1062:         cell_of[(zp, c)] = (name, bundle, rep)\n1063:     if rep:\n1064:         per = dict((a.get(\"S_c\") or {}).get(rep) or {})\n1065:         book[name] = book.get(name, 0.0) + sum(float(per.get(c, 0.0)) for c in kept)\n1066:     return mass, True\n1067: \n1068: \n1069: def _handoff_overlap_pieces(bundle: str, pieces: dict, to_district: dict, res: dict,\n1070:                             rank: dict, chans_of: dict, overlap_idx: dict, state_counts: dict,\n1071:                             rows_by_name: dict, d, cell_of: dict, book: dict) -> dict:\n1072:     \"\"\"Before `repair` runs: move every zip of a non-heaviest piece of `bundle` whose state it\n1073:     lost the overlap on to the winning bundle's own district, so the repair never tries to grow\n1074:     this bundle back into territory the plan handed to someone else.\n1075: \n1076:     A piece's state `s` is lost when some bundle processed earlier than `bundle` (`rank`) also\n1077:     carries one of `bundle`'s own channels there (`overlap_idx`, from `_overlaps`) and actually\n1078:     holds zips of `s` (`state_counts`, `{district: {state: n_zips}}`, updated here too, so a\n1079:     third bundle handed off after this one sees the up-to-date count).  Among all such earlier\n1080:     bundles' districts, the winner is the one holding the most zips of `s` (ties by name); the\n1081:     handed zip's cells move on whichever of `bundle`'s channels the winner also carries and had\n1082:     not yet claimed (`_claim_cells`).  `rows_by_name` is `{district: its districts.csv row}`,\n1083:     the winner's row already appended by its own earlier turn through this loop and mutated in\n1084:     place here rather than waiting for an accounting pass that has already run.\n1085: \n1086:     A district's heaviest piece is never a candidate, however deep into an overlap state it\n1087:     sits: only a detached piece is a hand-off's to give away.\n1088:     \"\"\"\n1089:     handed_off: dict[str, int] = {}\n1090:     handed_to: dict[str, int] = {}\n1091:     my_rank = rank[bundle]\n1092:     chans_b = chans_of[bundle]\n1093:     for name, parts in pieces.items():\n1094:         if name == OTHER or len(parts) <= 1:\n1095:             continue\n1096:         heaviest = max(parts, key=lambda p: sum(float(res[\"M_by_zip\"].get(z, 0.0)) for z in p))\n1097:         for p in parts:\n1098:             if p is heaviest:\n1099:                 continue\n1100:             for zp in p:\n1101:                 st = res[\"states_by_zip\"].get(zp, \"\")\n1102:                 winners = {w for c in chans_b for w in overlap_idx.get((st, c), ())\n1103:                           if w != bundle and rank.get(w, my_rank) < my_rank}\n1104:                 if not winners:\n1105:                     continue\n1106:                 best_n, best_name = -1, None\n1107:                 for wname, counts in state_counts.items():\n1108:                     if rows_by_name[wname][\"bundle\"] not in winners:\n1109:                         continue\n1110:                     n = counts.get(st, 0)\n1111:                     if n > 0 and (n > best_n or (n == best_n and wname < best_name)):\n1112:                         best_n, best_name = n, wname\n1113:                 if best_name is None:\n1114:                     continue\n1115:                 # the winner's own realise may already own every one of `zp`'s cells here (the\n1116:                 # pre-existing `cell_of` rule already resolved the overlap in its favour before\n1117:                 # this bundle's turn), in which case nothing new is claimed and its mass/n_zips/\n1118:                 # book, already counted on its own turn, must not be counted twice, but the\n1119:                 # zip still has to leave `to_district`, or it keeps inflating this bundle's own\n1120:                 # piece count with a piece that holds no cell at all\n1121:                 win_row = rows_by_name[best_name]\n1122:                 win_bundle, win_rep = win_row[\"bundle\"], win_row[\"wholesaler\"]\n1123:                 handoff_chans = [c for c in chans_b if c in chans_of[win_bundle]]\n1124:                 added, claimed = _claim_cells(d, cell_of, book, best_name, win_bundle, win_rep,\n1125:                                               zp, handoff_chans)\n1126:                 if claimed:\n1127:                     win_row[\"mass\"] += added\n1128:                     win_row[\"n_zips\"] += 1\n1129:                     counts = state_counts.setdefault(best_name, {})\n1130:                     counts[st] = counts.get(st, 0) + 1\n1131:                 handed_off[name] = handed_off.get(name, 0) + 1\n1132:                 handed_to[best_name] = handed_to.get(best_name, 0) + 1\n1133:                 del to_district[zp]\n1134:     return dict(handed_off=handed_off, handed_to=handed_to)\n1135: \n1136: \n1137: def _sweep_target(zp: str, cands: dict, dist_bundle: dict, bundle_graphs: dict) -> tuple:\n1138:     \"\"\"`(district, adjacent)` for one unclaimed cell of `zp`, choosing among `cands`\n1139:     (`{district: n_cells}`, all already serving this (state, channel)).\n1140: \n1141:     Adjacent: the candidate with a cell-graph edge from `zp` on its own bundle's final\n1142:     `to_district` labelling, most such edges then name.  With no adjacent candidate, the one\n1143:     already holding the most cells of this (state, channel), ties by name.\n1144:     \"\"\"\n1145:     adj = []\n1146:     for name in cands:\n1147:         G, to_district = bundle_graphs.get(dist_bundle[name], (None, None))\n1148:         if G is None or zp not in G:\n1149:             continue\n1150:         n = sum(1 for nb in G[zp] if to_district.get(nb) == name)\n1151:         if n > 0:\n1152:             adj.append((n, name))\n1153:     if adj:\n1154:         top = max(n for n, _ in adj)\n1155:         return min(name for n, name in adj if n == top), True\n1156:     top = max(cands.values())\n1157:     return min(name for name, n in cands.items() if n == top), False\n1158: \n1159: \n1160: def _sweep_zips(d, cell_of: dict, book: dict, rows_by_name: dict, bundle_graphs: dict) -> dict:\n1161:     \"\"\"Claim every (zip, fine channel) cell of positive mass no bundle claimed, onto a district\n1162:     that already holds a cell of that channel in the zip's own state.\n1163: \n1164:     The candidate index (`{(state, channel): {district: n_cells}}`) and each district's bundle\n1165:     and rep are read off `cell_of` once, before any sweep claim, so a cell this sweep claims\n1166:     does not itself become a candidate for the next: \"already serves this channel\" means before\n1167:     the sweep ran, not mid-sweep.  A (state, channel) no district serves at all is recorded in\n1168:     the returned `unswept`, one row per (state, channel), never per zip.\n1169: \n1170:     Mutates `cell_of`, `book` and the `mass`/`n_zips` of the receiving rows in `rows_by_name` in\n1171:     place, the same accounting `_claim_cells` and the per-district loop in `_main` already do.\n1172:     `pieces`/`contiguous` are left untouched: the receiver's graph measure was taken before the\n1173:     sweep, off its own bundle's cut, and a swept zip does not change what that cut looked like.\n1174:     \"\"\"\n1175:     serve: dict[tuple, dict[str, int]] = {}\n1176:     dist_bundle: dict[str, str] = {}\n1177:     dist_rep: dict[str, str] = {}\n1178:     held_zips: dict[str, set] = {}\n1179:     for (zp, c), (name, bundle, rep) in cell_of.items():\n1180:         st = d.G.nodes[zp].get(\"state\", \"\") if zp in d.G else \"\"\n1181:         s = serve.setdefault((st, c), {})\n1182:         s[name] = s.get(name, 0) + 1\n1183:         dist_bundle.setdefault(name, bundle)\n1184:         dist_rep.setdefault(name, rep)\n1185:         held_zips.setdefault(name, set()).add(zp)\n1186: \n1187:     swept: list = []\n1188:     unswept_idx: dict[tuple, dict] = {}\n1189:     for zp in sorted(d.G):\n1190:         a = d.G.nodes[zp]\n1191:         st = a.get(\"state\", \"\")\n1192:         M_c = dict(a.get(\"M_c\") or {})\n1193:         for c in channels.CHANNELS:\n1194:             m = float(M_c.get(c, 0.0))\n1195:             if m <= 0.0 or (zp, c) in cell_of:\n1196:                 continue\n1197:             cands = serve.get((st, c), {})\n1198:             if not cands:\n1199:                 rec = unswept_idx.setdefault((st, c), dict(state=st, channel=c, cells=0,\n1200:                                                            mass=0.0))\n1201:                 rec[\"cells\"] += 1\n1202:                 rec[\"mass\"] += m\n1203:                 continue\n1204:             name, adjacent = _sweep_target(zp, cands, dist_bundle, bundle_graphs)\n1205:             bundle, rep = dist_bundle[name], dist_rep[name]\n1206:             added, _ = _claim_cells(d, cell_of, book, name, bundle, rep, zp, [c])\n1207:             row = rows_by_name[name]\n1208:             row[\"mass\"] += added\n1209:             if zp not in held_zips.setdefault(name, set()):\n1210:                 row[\"n_zips\"] += 1\n1211:                 held_zips[name].add(zp)\n1212:             swept.append(dict(zip=zp, state=st, channel=c, district=name, bundle=bundle,\n1213:                               mass=added, adjacent=adjacent))\n1214: \n1215:     unswept = [dict(v, reason=\"no district serves this channel in the state\")\n1216:               for v in sorted(unswept_idx.values(), key=lambda r: (r[\"state\"], r[\"channel\"]))]\n1217:     by_district: dict[str, int] = {}\n1218:     for row in swept:\n1219:         by_district[row[\"district\"]] = by_district.get(row[\"district\"], 0) + 1\n1220:     return dict(\n1221:         swept=swept,\n1222:         sweep_zips=dict(cells=len(swept), mass=sum(r[\"mass\"] for r in swept),\n1223:                         by_district=dict(sorted(by_district.items())), unswept=unswept))\n1224: \n1225: \n1226: def _main(args) -> int:\n1227:     run_dir = os.path.abspath(args.run_dir)\n1228:     out = os.path.abspath(args.out or run_dir)\n1229:     os.makedirs(out, exist_ok=True)\n1230: \n1231:     plan, staffing, params = _load_run(run_dir)\n1232:     d = cell_instance(run_dir, params)\n1233:     by_bundle = used_by_bundle(plan)\n1234:     rep_of = wholesaler_of(staffing)\n1235:     # the plan's own mass band, in the projection's units.  A run whose params.json predates it\n1236:     # gets no band guard, and `realise.json` says so.\n1237:     band = ((float(params[\"L\"]), float(params[\"U\"]))\n1238:             if params.get(\"L\") is not None and params.get(\"U\") is not None else None)\n1239:     split_cut_bundles = {s.strip() for s in args.split_cut_bundles.split(\",\") if s.strip()}\n1240:     print(f\"plan: {sum(len(v) for v in by_bundle.values())} used slot(s) over \"\n1241:           f\"{len(by_bundle)} bundle(s) {sorted(by_bundle)}, {len(rep_of)} staffed, \"\n1242:           f\"band {'[%g, %g]' % band if band else 'unset'}\", flush=True)\n1243: \n1244:     record: dict[str, dict] = {}\n1245:     cell_of: dict[tuple, tuple] = {}\n1246:     district_rows: list[dict] = []\n1247:     rows_by_name: dict[str, dict] = {}\n1248:     book: dict[str, float] = {}\n1249:     state_counts: dict[str, dict] = {}          # {district: {state: n_zips}}, post-repair\n1250:     bundle_graphs: dict[str, tuple] = {}        # {bundle: (G, final to_district)}, for --sweep-zips\n1251: \n1252:     # bundles with more file channels claim their cells first, so where two projections cut the\n1253:     # same (zip, channel) the merged district keeps it.  A catch-all bundle `td.channels` does\n1254:     # not name counts 0 and goes last.  Channels are known for every bundle before any of them\n1255:     # runs, so a losing bundle can already tell which of the earlier bundles it overlaps with.\n1256:     order = sorted(by_bundle, key=lambda b: (-_n_file_channels(b), b))\n1257:     rank = {b: i for i, b in enumerate(order)}\n1258:     projs = {b: descaled.load_descaled(os.path.join(run_dir, \"projections\", b,\n1259:                                                     \"instance_descaled.json.gz\"))\n1260:              for b in order}\n1261:     chans_of = {b: bundle_channels(projs[b], b) for b in order}\n1262:     overlaps = _overlaps(by_bundle, chans_of)\n1263:     overlap_idx: dict[tuple, list] = {(o[\"state\"], o[\"channel\"]): o[\"bundles\"] for o in overlaps}\n1264: \n1265:     for bundle in order:\n1266:         t0 = time.time()\n1267:         recs = by_bundle[bundle]\n1268:         cell = os.path.join(run_dir, \"projections\", bundle)\n1269:         proj = projs[bundle]\n1270:         chans = chans_of[bundle]\n1271:         shares = read_shares(os.path.join(cell, \"state_shares.csv\"))\n1272:         prep = realise_prepare(bundle, recs, proj, shares, args.geo_cache)\n1273:         G, borders, gpath = cell_graph(run_dir, bundle, prep[\"placed\"], prep[\"xy_dict\"],\n1274:                                        prep[\"states_by_zip\"], args.geo_cache)\n1275:         cut = args.split_cut if bundle in split_cut_bundles else \"power\"\n1276:         res = realise_finish(bundle, recs, prep, args.rounds, G=G, split_cut=cut)\n1277:         to_district = res[\"to_district\"]\n1278: \n1279:         before = district_pieces(G, to_district)\n1280:         handoff = _handoff_overlap_pieces(bundle, before, to_district, res, rank, chans_of,\n1281:                                           overlap_idx, state_counts, rows_by_name, d, cell_of,\n1282:                                           book)\n1283:         band_b = band_of(params, bundle, band)\n1284:         slack_b = args.band_slack * 0.5 * (band_b[0] + band_b[1]) if band_b else 0.0\n1285:         fix = repair(G, to_district, res[\"M_by_zip\"], res[\"states_by_zip\"],\n1286:                      res[\"admissible\"], band_b, rounds=args.repair_rounds, slack=slack_b)\n1287:         to_district = fix[\"labels\"]\n1288:         bundle_graphs[bundle] = (G, to_district)\n1289:         after = district_pieces(G, to_district)\n1290:         sadj = state_adjacency(G, res[\"states_by_zip\"])\n1291:         # a state pair that shares a real border but no cell edge: the tessellation opened a\n1292:         # gap, and a district straddling it cannot be repaired to contiguity here\n1293:         on_graph = {tuple(sorted((a, b))) for a in sadj for b in sadj[a]}\n1294:         gaps = [b for b in borders if tuple(sorted(b)) not in on_graph]\n1295:         still = unrepaired(after, plan_states(res[\"admissible\"]), sadj, fix[\"stuck\"], gaps)\n1296:         pieces_of = {name: len(parts) for name, parts in after.items()}\n1297: \n1298:         members: dict[str, list] = {}\n1299:         for zp, name in to_district.items():\n1300:             members.setdefault(name, []).append(zp)\n1301:         for name, zips_ in members.items():\n1302:             if name == OTHER:\n1303:                 continue\n1304:             counts = state_counts.setdefault(name, {})\n1305:             for zp in zips_:\n1306:                 st = res[\"states_by_zip\"].get(zp, \"\")\n1307:                 counts[st] = counts.get(st, 0) + 1\n1308: \n1309:         for j, (idx, rec) in enumerate(recs):\n1310:             name = district_name(bundle, j)\n1311:             zips_j = sorted(members.get(name, ()))\n1312:             rep = rep_of.get(idx, \"\")\n1313:             mass, held = 0.0, 0\n1314:             for zp in zips_j:\n1315:                 # a zip of a state the plan gives this district no share of, holding no mass\n1316:                 # in this projection, is one `centers.assign` parked here (the TODO in\n1317:                 # `off_plan`) after an earlier stage took the state's channels.  It claims no\n1318:                 # cell and adds no mass, so the bundle that does hold it is the one\n1319:                 # `assignment.csv` names.  A zero-mass zip inside the district's own states\n1320:                 # stays: a territory is an area, not only its sold zips.\n1321:                 if (zp not in proj.G or float(proj.G.nodes[zp].get(\"M\", 0.0)) <= 0.0) and \\\n1322:                         name not in res[\"admissible\"].get(res[\"states_by_zip\"].get(zp, \"\"),\n1323:                                                           set()):\n1324:                     continue\n1325:                 # a cell a bundle with more file channels already claimed stays with it (the\n1326:                 # loop order above); only the cells kept count towards this district\n1327:                 added, claimed = _claim_cells(d, cell_of, book, name, bundle, rep, zp, chans)\n1328:                 if not claimed:\n1329:                     continue\n1330:                 held += 1\n1331:                 mass += added\n1332:             row = dict(\n1333:                 district=name, slot=rec[\"id\"], bundle=bundle, channels=chans,\n1334:                 states=\",\".join(f\"{st}:{sh:g}\" for st, sh in sorted(rec[\"y\"].items())),\n1335:                 n_zips=held, mass=mass, wholesaler=rep, staffed=bool(rep),\n1336:                 pieces=pieces_of.get(name, 0),\n1337:                 contiguous=pieces_of.get(name, 0) == 1)\n1338:             district_rows.append(row)\n1339:             rows_by_name[name] = row\n1340: \n1341:         resid_mass = 0.0\n1342:         for zp in members.get(OTHER, ()):\n1343:             M_c = dict(d.G.nodes[zp].get(\"M_c\") or {})\n1344:             resid_mass += sum(float(M_c.get(c, 0.0)) for c in chans)\n1345:         names = [district_name(bundle, j) for j in range(len(recs))]\n1346:         record[bundle] = dict(\n1347:             status=\"ok\", k=len(recs), n_zips=len(to_district),\n1348:             split_states=res[\"split_states\"], rounds_used=res[\"rounds_used\"],\n1349:             n_fractional=res[\"n_fractional\"], pseudo_column=res[\"pseudo_column\"],\n1350:             folded_states=res[\"folded_states\"], n_missing=res[\"n_missing\"],\n1351:             residual_zips=len(members.get(OTHER, ())), residual_mass=resid_mass,\n1352:             graph=GRAPH, graph_path=os.path.relpath(gpath, run_dir),\n1353:             graph_zips=G.number_of_nodes(), graph_edges=G.number_of_edges(),\n1354:             components=nx.number_connected_components(G),\n1355:             cross_state_edges=sum(1 for a, b in G.edges()\n1356:                                   if res[\"states_by_zip\"].get(a) != res[\"states_by_zip\"].get(b)),\n1357:             no_cell_zips=len([z for z in res[\"placed\"] if z not in G]),\n1358:             state_borders=len(borders), state_borders_on_graph=len(on_graph),\n1359:             state_borders_missing=gaps,\n1360:             band=list(band_b) if band_b else None, band_slack=slack_b,\n1361:             pieces_before={n: len(before.get(n, ())) for n in names},\n1362:             pieces_after={n: len(after.get(n, ())) for n in names},\n1363:             moved=fix[\"moved\"], bridged=fix[\"bridged\"], swapped=fix[\"swapped\"],\n1364:             repair_rounds=fix[\"rounds_used\"],\n1365:             off_plan=fix[\"off_plan\"],\n1366:             band_violations=[dict(district=n, mass=fix[\"mass\"].get(n, 0.0))\n1367:                              for n in names if _excess(fix[\"mass\"].get(n, 0.0), band_b) > 0],\n1368:             unrepaired=still,\n1369:             handed_off=handoff[\"handed_off\"], handed_to=handoff[\"handed_to\"],\n1370:             split_cut=res[\"split_cut\"], cut_deviation=res[\"cut_deviation\"],\n1371:             seconds=time.time() - t0)\n1372:         rec_b = record[bundle]\n1373:         print(f\"{bundle}: {len(recs)} district(s), {len(res['split_states'])} split state(s), \"\n1374:               f\"{rec_b['residual_zips']} zip(s) other; graph {GRAPH} \"\n1375:               f\"{rec_b['graph_zips']} zip(s) {rec_b['graph_edges']} edge(s) \"\n1376:               f\"{rec_b['components']} component(s), {rec_b['cross_state_edges']} cross-state; \"\n1377:               f\"pieces {sum(rec_b['pieces_before'].values())} -> \"\n1378:               f\"{sum(rec_b['pieces_after'].values())}, {fix['moved']} zip(s) moved, \"\n1379:               f\"{fix['bridged']} bridge(s) ({fix['swapped']} zip(s) swapped), \"\n1380:               f\"{fix['off_plan']['zips']} off-plan (mass {fix['off_plan']['mass']:g}), \"\n1381:               f\"{sum(handoff['handed_off'].values())} handed off, \"\n1382:               f\"{len(still)} district(s) still split \"\n1383:               f\"({rec_b['seconds']:.1f}s)\", flush=True)\n1384: \n1385:     if overlaps:\n1386:         print(f\"warning: {len(overlaps)} (state, channel) cell(s) served by two bundles; the \"\n1387:               f\"bundle with more file channels wins\", flush=True)\n1388: \n1389:     sweep = None\n1390:     if args.sweep_zips:\n1391:         sweep = _sweep_zips(d, cell_of, book, rows_by_name, bundle_graphs)\n1392:         sz = sweep[\"sweep_zips\"]\n1393:         n_adjacent = sum(1 for r in sweep[\"swept\"] if r[\"adjacent\"])\n1394:         unswept_mass = sum(u[\"mass\"] for u in sz[\"unswept\"])\n1395:         print(f\"sweep: {sz['cells']} cell(s) swept (mass {sz['mass']:g}, {n_adjacent} adjacent) \"\n1396:               f\"across {len(sz['by_district'])} district(s); {len(sz['unswept'])} (state, \"\n1397:               f\"channel) group(s) left unswept (mass {unswept_mass:g})\", flush=True)\n1398: \n1399:     assigned, residual = _write_assignment(os.path.join(out, \"assignment.csv\"), d, cell_of)\n1400:     _write_districts(os.path.join(out, \"districts.csv\"), district_rows)\n1401: \n1402:     total = {}\n1403:     for zp in d.G:\n1404:         for rep, v in (d.G.nodes[zp].get(\"S\") or {}).items():\n1405:             total[rep] = total.get(rep, 0.0) + float(v)\n1406:     held = {r[\"wholesaler\"]: r for r in district_rows if r[\"wholesaler\"]}\n1407:     wrows = []\n1408:     for rep in sorted(set(total) | set(staffing.get(\"reps\") or ()) | set(held)):\n1409:         r = held.get(rep)\n1410:         b_in = book.get(r[\"district\"], 0.0) if r else 0.0\n1411:         b_all = total.get(rep, 0.0)\n1412:         wrows.append(dict(wholesaler=rep, district=r[\"district\"] if r else \"\",\n1413:                           bundle=r[\"bundle\"] if r else \"\", n_zips=r[\"n_zips\"] if r else 0,\n1414:                           book_in_district=b_in, book_total=b_all,\n1415:                           share_of_book_kept=(b_in / b_all) if b_all > 0 else 0.0))\n1416:     _write_wholesalers(os.path.join(out, \"wholesalers.csv\"), wrows)\n1417: \n1418:     with open(os.path.join(out, \"realise.json\"), \"w\", encoding=\"utf-8\") as fh:\n1419:         json.dump(dict(run_dir=run_dir, rounds=args.rounds,\n1420:                        repair_rounds=args.repair_rounds, graph=GRAPH,\n1421:                        split_cut=args.split_cut, split_cut_bundles=sorted(split_cut_bundles),\n1422:                        band=list(band) if band else None, bundles=record,\n1423:                        overlaps=overlaps,\n1424:                        swept=sweep[\"swept\"] if sweep else [],\n1425:                        sweep_zips=sweep[\"sweep_zips\"] if sweep else None,\n1426:                        districts=[dict(district=r[\"district\"], slot=r[\"slot\"],\n1427:                                        bundle=r[\"bundle\"], wholesaler=r[\"wholesaler\"])\n1428:                                   for r in district_rows]),\n1429:                   fh, indent=2, default=float)\n1430:         fh.write(\"\\n\")\n1431: \n1432:     staffed = sum(1 for r in district_rows if r[\"staffed\"])\n1433:     print(f\"districts={len(district_rows)} staffed={staffed} \"\n1434:           f\"unstaffed={len(district_rows) - staffed} \"\n1435:           f\"disconnected={sum(1 for r in district_rows if not r['contiguous'])} \"\n1436:           f\"moved={sum(v['moved'] for v in record.values())} \"\n1437:           f\"band_violations={sum(len(v['band_violations']) for v in record.values())}\",\n1438:           flush=True)\n1439:     for bundle in sorted(record):\n1440:         for row in record[bundle][\"unrepaired\"]:\n1441:             print(f\"  {row['district']}: {row['pieces']} pieces, \"\n1442:                   f\"{row['detached_zips']} zip(s) detached -- {row['reason']}\", flush=True)\n1443:     for c in channels.CHANNELS:\n1444:         print(f\"{c}: {assigned[c]} zip(s) assigned, residual mass {residual[c]:.6g}\",\n1445:               flush=True)\n1446:     print(f\"wrote {out}\", flush=True)\n1447:     return 0\n1448: \n1449: \n1450: def main(argv=None) -> int:\n1451:     args = build_argparser().parse_args(argv)\n1452:     return _main(args)\n1453: \n1454: \n1455: if __name__ == \"__main__\":\n1456:     raise SystemExit(main())\n\n(End of file - total 1456 lines)\n</content>","metadata":{"preview":"    A named bundle leaves its name there and a catch-all bundle its channel list, so this is\n    the one reader that covers both without importing the driver that made them.\n    \"\"\"\n    named = proj.meta.get(\"bundle\")\n    if isinstance(named, str):\n        named = channels.BUNDLES.get(named)\n    chans = tuple(named or ())\n    unknown = [c for c in chans if c not in channels.CHANNELS]\n    if not chans or unknown:\n        raise ValueError(f\"bundle {bundle!r}: the projection's meta['bundle'] is \"\n                         f\"{proj.meta.get('bundle')!r}, not a channel list this run can key \"\n                         f\"a cell table by\")\n    return chans\n\n\ndef _overlaps(by_bundle: dict, chans_of: dict) -> list:\n    \"\"\"(state, channel) pairs two used bundles both take a share of.  Level 0's cover row is\n    `<= 1` per cell summed over slots, so this is a plan the projections cannot express.\"\"\"\n    seen: dict[tuple, set] = {}\n    for bundle, recs in by_bundle.items():","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/tools/plan_realise.py","text":"    A named bundle leaves its name there and a catch-all bundle its channel list, so this is\n    the one reader that covers both without importing the driver that made them.\n    \"\"\"\n    named = proj.meta.get(\"bundle\")\n    if isinstance(named, str):\n        named = channels.BUNDLES.get(named)\n    chans = tuple(named or ())\n    unknown = [c for c in chans if c not in channels.CHANNELS]\n    if not chans or unknown:\n        raise ValueError(f\"bundle {bundle!r}: the projection's meta['bundle'] is \"\n                         f\"{proj.meta.get('bundle')!r}, not a channel list this run can key \"\n                         f\"a cell table by\")\n    return chans\n\n\ndef _overlaps(by_bundle: dict, chans_of: dict) -> list:\n    \"\"\"(state, channel) pairs two used bundles both take a share of.  Level 0's cover row is\n    `<= 1` per cell summed over slots, so this is a plan the projections cannot express.\"\"\"\n    seen: dict[tuple, set] = {}\n    for bundle, recs in by_bundle.items():\n        for c in chans_of.get(bundle, ()):\n            for _, rec in recs:\n                for st in rec[\"y\"]:\n                    seen.setdefault((st, c), set()).add(bundle)\n    return [dict(state=st, channel=c, bundles=sorted(bs))\n            for (st, c), bs in sorted(seen.items()) if len(bs) > 1]\n\n\ndef band_of(params: dict, bundle: str, default):\n    \"\"\"The band the plan held this bundle's districts to.  Under `--band-mode per-bundle` the\n    driver records one band per bundle (`params[\"bands\"][bundle]`, tau_B = M_B / k_B), and that\n    is the band the repair must guard; `default` (the file-level `L`, `U`) is the band of a run\n    without per-bundle bands.\"\"\"\n    rec = (params.get(\"bands\") or {}).get(bundle) or {}\n    if rec.get(\"L\") is not None and rec.get(\"U\") is not None:\n        return (float(rec[\"L\"]), float(rec[\"U\"]))\n    return default\n\n\ndef _n_file_channels(bundle: str) -> int:\n    \"\"\"How many of the three file channels a bundle carries: WHFI_PLUS 3; WHFI, WH_PLUS and\n    FI_PLUS 2; WH, FI and N 1.  N's two fine labels are one file channel, so it does not\n    outrank a pure bundle.\"\"\"\n    return len({FILE_OF[c] for c in channels.BUNDLES.get(bundle, ())})\n\n\ndef _claim_cells(d, cell_of: dict, book: dict, name: str, bundle: str, rep: str, zp: str,\n                 chans) -> tuple:\n    \"\"\"Claim `zp`'s cells on `chans` not already in `cell_of`, crediting `name`'s mass and, when\n    `rep` holds book there, `book[name]`.  Returns `(mass_added, claimed)`; `claimed` is\n    `False` when every channel in `chans` was already claimed, so the caller counts no zip.\n\n    The one place a cell actually changes hands, shared by the per-district accounting loop in\n    `_main` and `_handoff_overlap_pieces`.\n    \"\"\"\n    kept = [c for c in chans if (zp, c) not in cell_of]\n    if not kept:\n        return 0.0, False\n    a = d.G.nodes[zp]\n    M_c = dict(a.get(\"M_c\") or {})\n    mass = sum(float(M_c.get(c, 0.0)) for c in kept)\n    for c in kept:\n        cell_of[(zp, c)] = (name, bundle, rep)\n    if rep:\n        per = dict((a.get(\"S_c\") or {}).get(rep) or {})\n        book[name] = book.get(name, 0.0) + sum(float(per.get(c, 0.0)) for c in kept)\n    return mass, True\n\n\ndef _handoff_overlap_pieces(bundle: str, pieces: dict, to_district: dict, res: dict,\n                            rank: dict, chans_of: dict, overlap_idx: dict, state_counts: dict,\n                            rows_by_name: dict, d, cell_of: dict, book: dict) -> dict:\n    \"\"\"Before `repair` runs: move every zip of a non-heaviest piece of `bundle` whose state it\n    lost the overlap on to the winning bundle's own district, so the repair never tries to grow\n    this bundle back into territory the plan handed to someone else.\n\n    A piece's state `s` is lost when some bundle processed earlier than `bundle` (`rank`) also\n    carries one of `bundle`'s own channels there (`overlap_idx`, from `_overlaps`) and actually\n    holds zips of `s` (`state_counts`, `{district: {state: n_zips}}`, updated here too, so a\n    third bundle handed off after this one sees the up-to-date count).  Among all such earlier\n    bundles' districts, the winner is the one holding the most zips of `s` (ties by name); the\n    handed zip's cells move on whichever of `bundle`'s channels the winner also carries and had\n    not yet claimed (`_claim_cells`).  `rows_by_name` is `{district: its districts.csv row}`,\n    the winner's row already appended by its own earlier turn through this loop and mutated in\n    place here rather than waiting for an accounting pass that has already run.\n\n    A district's heaviest piece is never a candidate, however deep into an overlap state it\n    sits: only a detached piece is a hand-off's to give away.\n    \"\"\"\n    handed_off: dict[str, int] = {}\n    handed_to: dict[str, int] = {}\n    my_rank = rank[bundle]\n    chans_b = chans_of[bundle]\n    for name, parts in pieces.items():\n        if name == OTHER or len(parts) <= 1:\n            continue\n        heaviest = max(parts, key=lambda p: sum(float(res[\"M_by_zip\"].get(z, 0.0)) for z in p))\n        for p in parts:\n            if p is heaviest:\n                continue\n            for zp in p:\n                st = res[\"states_by_zip\"].get(zp, \"\")\n                winners = {w for c in chans_b for w in overlap_idx.get((st, c), ())\n                          if w != bundle and rank.get(w, my_rank) < my_rank}\n                if not winners:\n                    continue\n                best_n, best_name = -1, None\n                for wname, counts in state_counts.items():\n                    if rows_by_name[wname][\"bundle\"] not in winners:\n                        continue\n                    n = counts.get(st, 0)\n                    if n > 0 and (n > best_n or (n == best_n and wname < best_name)):\n                        best_n, best_name = n, wname\n                if best_name is None:\n                    continue\n                # the winner's own realise may already own every one of `zp`'s cells here (the\n                # pre-existing `cell_of` rule already resolved the overlap in its favour before\n                # this bundle's turn), in which case nothing new is claimed and its mass/n_zips/\n                # book, already counted on its own turn, must not be counted twice, but the\n                # zip still has to leave `to_district`, or it keeps inflating this bundle's own\n                # piece count with a piece that holds no cell at all\n                win_row = rows_by_name[best_name]\n                win_bundle, win_rep = win_row[\"bundle\"], win_row[\"wholesaler\"]\n                handoff_chans = [c for c in chans_b if c in chans_of[win_bundle]]\n                added, claimed = _claim_cells(d, cell_of, book, best_name, win_bundle, win_rep,\n                                              zp, handoff_chans)\n                if claimed:\n                    win_row[\"mass\"] += added\n                    win_row[\"n_zips\"] += 1\n                    counts = state_counts.setdefault(best_name, {})\n                    counts[st] = counts.get(st, 0) + 1\n                handed_off[name] = handed_off.get(name, 0) + 1\n                handed_to[best_name] = handed_to.get(best_name, 0) + 1\n                del to_district[zp]\n    return dict(handed_off=handed_off, handed_to=handed_to)\n\n\ndef _sweep_target(zp: str, cands: dict, dist_bundle: dict, bundle_graphs: dict) -> tuple:\n    \"\"\"`(district, adjacent)` for one unclaimed cell of `zp`, choosing among `cands`\n    (`{district: n_cells}`, all already serving this (state, channel)).\n\n    Adjacent: the candidate with a cell-graph edge from `zp` on its own bundle's final\n    `to_district` labelling, most such edges then name.  With no adjacent candidate, the one\n    already holding the most cells of this (state, channel), ties by name.\n    \"\"\"\n    adj = []\n    for name in cands:\n        G, to_district = bundle_graphs.get(dist_bundle[name], (None, None))\n        if G is None or zp not in G:\n            continue\n        n = sum(1 for nb in G[zp] if to_district.get(nb) == name)\n        if n > 0:\n            adj.append((n, name))\n    if adj:\n        top = max(n for n, _ in adj)\n        return min(name for n, name in adj if n == top), True\n    top = max(cands.values())\n    return min(name for name, n in cands.items() if n == top), False\n\n\ndef _sweep_zips(d, cell_of: dict, book: dict, rows_by_name: dict, bundle_graphs: dict) -> dict:\n    \"\"\"Claim every (zip, fine channel) cell of positive mass no bundle claimed, onto a district\n    that already holds a cell of that channel in the zip's own state.\n\n    The candidate index (`{(state, channel): {district: n_cells}}`) and each district's bundle\n    and rep are read off `cell_of` once, before any sweep claim, so a cell this sweep claims\n    does not itself become a candidate for the next: \"already serves this channel\" means before\n    the sweep ran, not mid-sweep.  A (state, channel) no district serves at all is recorded in\n    the returned `unswept`, one row per (state, channel), never per zip.\n\n    Mutates `cell_of`, `book` and the `mass`/`n_zips` of the receiving rows in `rows_by_name` in\n    place, the same accounting `_claim_cells` and the per-district loop in `_main` already do.\n    `pieces`/`contiguous` are left untouched: the receiver's graph measure was taken before the\n    sweep, off its own bundle's cut, and a swept zip does not change what that cut looked like.\n    \"\"\"\n    serve: dict[tuple, dict[str, int]] = {}\n    dist_bundle: dict[str, str] = {}\n    dist_rep: dict[str, str] = {}\n    held_zips: dict[str, set] = {}\n    for (zp, c), (name, bundle, rep) in cell_of.items():\n        st = d.G.nodes[zp].get(\"state\", \"\") if zp in d.G else \"\"\n        s = serve.setdefault((st, c), {})\n        s[name] = s.get(name, 0) + 1\n        dist_bundle.setdefault(name, bundle)\n        dist_rep.setdefault(name, rep)\n        held_zips.setdefault(name, set()).add(zp)\n\n    swept: list = []\n    unswept_idx: dict[tuple, dict] = {}\n    for zp in sorted(d.G):\n        a = d.G.nodes[zp]\n        st = a.get(\"state\", \"\")\n        M_c = dict(a.get(\"M_c\") or {})\n        for c in channels.CHANNELS:\n            m = float(M_c.get(c, 0.0))\n            if m <= 0.0 or (zp, c) in cell_of:\n                continue\n            cands = serve.get((st, c), {})\n            if not cands:\n                rec = unswept_idx.setdefault((st, c), dict(state=st, channel=c, cells=0,\n                                                           mass=0.0))\n                rec[\"cells\"] += 1\n                rec[\"mass\"] += m\n                continue\n            name, adjacent = _sweep_target(zp, cands, dist_bundle, bundle_graphs)\n            bundle, rep = dist_bundle[name], dist_rep[name]\n            added, _ = _claim_cells(d, cell_of, book, name, bundle, rep, zp, [c])\n            row = rows_by_name[name]\n            row[\"mass\"] += added\n            if zp not in held_zips.setdefault(name, set()):\n                row[\"n_zips\"] += 1\n                held_zips[name].add(zp)\n            swept.append(dict(zip=zp, state=st, channel=c, district=name, bundle=bundle,\n                              mass=added, adjacent=adjacent))\n\n    unswept = [dict(v, reason=\"no district serves this channel in the state\")\n              for v in sorted(unswept_idx.values(), key=lambda r: (r[\"state\"], r[\"channel\"]))]\n    by_district: dict[str, int] = {}\n    for row in swept:\n        by_district[row[\"district\"]] = by_district.get(row[\"district\"], 0) + 1\n    return dict(\n        swept=swept,\n        sweep_zips=dict(cells=len(swept), mass=sum(r[\"mass\"] for r in swept),\n                        by_district=dict(sorted(by_district.items())), unswept=unswept))\n\n\ndef _main(args) -> int:\n    run_dir = os.path.abspath(args.run_dir)\n    out = os.path.abspath(args.out or run_dir)\n    os.makedirs(out, exist_ok=True)\n\n    plan, staffing, params = _load_run(run_dir)\n    d = cell_instance(run_dir, params)\n    by_bundle = used_by_bundle(plan)\n    rep_of = wholesaler_of(staffing)\n    # the plan's own mass band, in the projection's units.  A run whose params.json predates it\n    # gets no band guard, and `realise.json` says so.\n    band = ((float(params[\"L\"]), float(params[\"U\"]))\n            if params.get(\"L\") is not None and params.get(\"U\") is not None else None)\n    split_cut_bundles = {s.strip() for s in args.split_cut_bundles.split(\",\") if s.strip()}\n    print(f\"plan: {sum(len(v) for v in by_bundle.values())} used slot(s) over \"\n          f\"{len(by_bundle)} bundle(s) {sorted(by_bundle)}, {len(rep_of)} staffed, \"\n          f\"band {'[%g, %g]' % band if band else 'unset'}\", flush=True)\n\n    record: dict[str, dict] = {}\n    cell_of: dict[tuple, tuple] = {}\n    district_rows: list[dict] = []\n    rows_by_name: dict[str, dict] = {}\n    book: dict[str, float] = {}\n    state_counts: dict[str, dict] = {}          # {district: {state: n_zips}}, post-repair\n    bundle_graphs: dict[str, tuple] = {}        # {bundle: (G, final to_district)}, for --sweep-zips\n\n    # bundles with more file channels claim their cells first, so where two projections cut the\n    # same (zip, channel) the merged district keeps it.  A catch-all bundle `td.channels` does\n    # not name counts 0 and goes last.  Channels are known for every bundle before any of them\n    # runs, so a losing bundle can already tell which of the earlier bundles it overlaps with.\n    order = sorted(by_bundle, key=lambda b: (-_n_file_channels(b), b))\n    rank = {b: i for i, b in enumerate(order)}\n    projs = {b: descaled.load_descaled(os.path.join(run_dir, \"projections\", b,\n                                                    \"instance_descaled.json.gz\"))\n             for b in order}\n    chans_of = {b: bundle_channels(projs[b], b) for b in order}\n    overlaps = _overlaps(by_bundle, chans_of)\n    overlap_idx: dict[tuple, list] = {(o[\"state\"], o[\"channel\"]): o[\"bundles\"] for o in overlaps}\n\n    for bundle in order:\n        t0 = time.time()\n        recs = by_bundle[bundle]\n        cell = os.path.join(run_dir, \"projections\", bundle)\n        proj = projs[bundle]\n        chans = chans_of[bundle]\n        shares = read_shares(os.path.join(cell, \"state_shares.csv\"))\n        prep = realise_prepare(bundle, recs, proj, shares, args.geo_cache)\n        G, borders, gpath = cell_graph(run_dir, bundle, prep[\"placed\"], prep[\"xy_dict\"],\n                                       prep[\"states_by_zip\"], args.geo_cache)\n        cut = args.split_cut if bundle in split_cut_bundles else \"power\"\n        res = realise_finish(bundle, recs, prep, args.rounds, G=G, split_cut=cut)\n        to_district = res[\"to_district\"]\n\n        before = district_pieces(G, to_district)\n        handoff = _handoff_overlap_pieces(bundle, before, to_district, res, rank, chans_of,\n                                          overlap_idx, state_counts, rows_by_name, d, cell_of,\n                                          book)\n        band_b = band_of(params, bundle, band)\n        slack_b = args.band_slack * 0.5 * (band_b[0] + band_b[1]) if band_b else 0.0\n        fix = repair(G, to_district, res[\"M_by_zip\"], res[\"states_by_zip\"],\n                     res[\"admissible\"], band_b, rounds=args.repair_rounds, slack=slack_b)\n        to_district = fix[\"labels\"]\n        bundle_graphs[bundle] = (G, to_district)\n        after = district_pieces(G, to_district)\n        sadj = state_adjacency(G, res[\"states_by_zip\"])\n        # a state pair that shares a real border but no cell edge: the tessellation opened a\n        # gap, and a district straddling it cannot be repaired to contiguity here\n        on_graph = {tuple(sorted((a, b))) for a in sadj for b in sadj[a]}\n        gaps = [b for b in borders if tuple(sorted(b)) not in on_graph]\n        still = unrepaired(after, plan_states(res[\"admissible\"]), sadj, fix[\"stuck\"], gaps)\n        pieces_of = {name: len(parts) for name, parts in after.items()}\n\n        members: dict[str, list] = {}\n        for zp, name in to_district.items():\n            members.setdefault(name, []).append(zp)\n        for name, zips_ in members.items():\n            if name == OTHER:\n                continue\n            counts = state_counts.setdefault(name, {})\n            for zp in zips_:\n                st = res[\"states_by_zip\"].get(zp, \"\")\n                counts[st] = counts.get(st, 0) + 1\n\n        for j, (idx, rec) in enumerate(recs):\n            name = district_name(bundle, j)\n            zips_j = sorted(members.get(name, ()))\n            rep = rep_of.get(idx, \"\")\n            mass, held = 0.0, 0\n            for zp in zips_j:\n                # a zip of a state the plan gives this district no share of, holding no mass\n                # in this projection, is one `centers.assign` parked here (the TODO in\n                # `off_plan`) after an earlier stage took the state's channels.  It claims no\n                # cell and adds no mass, so the bundle that does hold it is the one\n                # `assignment.csv` names.  A zero-mass zip inside the district's own states\n                # stays: a territory is an area, not only its sold zips.\n                if (zp not in proj.G or float(proj.G.nodes[zp].get(\"M\", 0.0)) <= 0.0) and \\\n                        name not in res[\"admissible\"].get(res[\"states_by_zip\"].get(zp, \"\"),\n                                                          set()):\n                    continue\n                # a cell a bundle with more file channels already claimed stays with it (the\n                # loop order above); only the cells kept count towards this district\n                added, claimed = _claim_cells(d, cell_of, book, name, bundle, rep, zp, chans)\n                if not claimed:\n                    continue\n                held += 1\n                mass += added\n            row = dict(\n                district=name, slot=rec[\"id\"], bundle=bundle, channels=chans,\n                states=\",\".join(f\"{st}:{sh:g}\" for st, sh in sorted(rec[\"y\"].items())),\n                n_zips=held, mass=mass, wholesaler=rep, staffed=bool(rep),\n                pieces=pieces_of.get(name, 0),\n                contiguous=pieces_of.get(name, 0) == 1)\n            district_rows.append(row)\n            rows_by_name[name] = row\n\n        resid_mass = 0.0\n        for zp in members.get(OTHER, ()):\n            M_c = dict(d.G.nodes[zp].get(\"M_c\") or {})\n            resid_mass += sum(float(M_c.get(c, 0.0)) for c in chans)\n        names = [district_name(bundle, j) for j in range(len(recs))]\n        record[bundle] = dict(\n            status=\"ok\", k=len(recs), n_zips=len(to_district),\n            split_states=res[\"split_states\"], rounds_used=res[\"rounds_used\"],\n            n_fractional=res[\"n_fractional\"], pseudo_column=res[\"pseudo_column\"],\n            folded_states=res[\"folded_states\"], n_missing=res[\"n_missing\"],\n            residual_zips=len(members.get(OTHER, ())), residual_mass=resid_mass,\n            graph=GRAPH, graph_path=os.path.relpath(gpath, run_dir),\n            graph_zips=G.number_of_nodes(), graph_edges=G.number_of_edges(),\n            components=nx.number_connected_components(G),\n            cross_state_edges=sum(1 for a, b in G.edges()\n                                  if res[\"states_by_zip\"].get(a) != res[\"states_by_zip\"].get(b)),\n            no_cell_zips=len([z for z in res[\"placed\"] if z not in G]),\n            state_borders=len(borders), state_borders_on_graph=len(on_graph),\n            state_borders_missing=gaps,\n            band=list(band_b) if band_b else None, band_slack=slack_b,\n            pieces_before={n: len(before.get(n, ())) for n in names},\n            pieces_after={n: len(after.get(n, ())) for n in names},\n            moved=fix[\"moved\"], bridged=fix[\"bridged\"], swapped=fix[\"swapped\"],\n            repair_rounds=fix[\"rounds_used\"],\n            off_plan=fix[\"off_plan\"],\n            band_violations=[dict(district=n, mass=fix[\"mass\"].get(n, 0.0))\n                             for n in names if _excess(fix[\"mass\"].get(n, 0.0), band_b) > 0],\n            unrepaired=still,\n            handed_off=handoff[\"handed_off\"], handed_to=handoff[\"handed_to\"],\n            split_cut=res[\"split_cut\"], cut_deviation=res[\"cut_deviation\"],\n            seconds=time.time() - t0)\n        rec_b = record[bundle]\n        print(f\"{bundle}: {len(recs)} district(s), {len(res['split_states'])} split state(s), \"\n              f\"{rec_b['residual_zips']} zip(s) other; graph {GRAPH} \"\n              f\"{rec_b['graph_zips']} zip(s) {rec_b['graph_edges']} edge(s) \"\n              f\"{rec_b['components']} component(s), {rec_b['cross_state_edges']} cross-state; \"\n              f\"pieces {sum(rec_b['pieces_before'].values())} -> \"\n              f\"{sum(rec_b['pieces_after'].values())}, {fix['moved']} zip(s) moved, \"\n              f\"{fix['bridged']} bridge(s) ({fix['swapped']} zip(s) swapped), \"\n              f\"{fix['off_plan']['zips']} off-plan (mass {fix['off_plan']['mass']:g}), \"\n              f\"{sum(handoff['handed_off'].values())} handed off, \"\n              f\"{len(still)} district(s) still split \"\n              f\"({rec_b['seconds']:.1f}s)\", flush=True)\n\n    if overlaps:\n        print(f\"warning: {len(overlaps)} (state, channel) cell(s) served by two bundles; the \"\n              f\"bundle with more file channels wins\", flush=True)\n\n    sweep = None\n    if args.sweep_zips:\n        sweep = _sweep_zips(d, cell_of, book, rows_by_name, bundle_graphs)\n        sz = sweep[\"sweep_zips\"]\n        n_adjacent = sum(1 for r in sweep[\"swept\"] if r[\"adjacent\"])\n        unswept_mass = sum(u[\"mass\"] for u in sz[\"unswept\"])\n        print(f\"sweep: {sz['cells']} cell(s) swept (mass {sz['mass']:g}, {n_adjacent} adjacent) \"\n              f\"across {len(sz['by_district'])} district(s); {len(sz['unswept'])} (state, \"\n              f\"channel) group(s) left unswept (mass {unswept_mass:g})\", flush=True)\n\n    assigned, residual = _write_assignment(os.path.join(out, \"assignment.csv\"), d, cell_of)\n    _write_districts(os.path.join(out, \"districts.csv\"), district_rows)\n\n    total = {}\n    for zp in d.G:\n        for rep, v in (d.G.nodes[zp].get(\"S\") or {}).items():\n            total[rep] = total.get(rep, 0.0) + float(v)\n    held = {r[\"wholesaler\"]: r for r in district_rows if r[\"wholesaler\"]}\n    wrows = []\n    for rep in sorted(set(total) | set(staffing.get(\"reps\") or ()) | set(held)):\n        r = held.get(rep)\n        b_in = book.get(r[\"district\"], 0.0) if r else 0.0\n        b_all = total.get(rep, 0.0)\n        wrows.append(dict(wholesaler=rep, district=r[\"district\"] if r else \"\",\n                          bundle=r[\"bundle\"] if r else \"\", n_zips=r[\"n_zips\"] if r else 0,\n                          book_in_district=b_in, book_total=b_all,\n                          share_of_book_kept=(b_in / b_all) if b_all > 0 else 0.0))\n    _write_wholesalers(os.path.join(out, \"wholesalers.csv\"), wrows)\n\n    with open(os.path.join(out, \"realise.json\"), \"w\", encoding=\"utf-8\") as fh:\n        json.dump(dict(run_dir=run_dir, rounds=args.rounds,\n                       repair_rounds=args.repair_rounds, graph=GRAPH,\n                       split_cut=args.split_cut, split_cut_bundles=sorted(split_cut_bundles),\n                       band=list(band) if band else None, bundles=record,\n                       overlaps=overlaps,\n                       swept=sweep[\"swept\"] if sweep else [],\n                       sweep_zips=sweep[\"sweep_zips\"] if sweep else None,\n                       districts=[dict(district=r[\"district\"], slot=r[\"slot\"],\n                                       bundle=r[\"bundle\"], wholesaler=r[\"wholesaler\"])\n                                  for r in district_rows]),\n                  fh, indent=2, default=float)\n        fh.write(\"\\n\")\n\n    staffed = sum(1 for r in district_rows if r[\"staffed\"])\n    print(f\"districts={len(district_rows)} staffed={staffed} \"\n          f\"unstaffed={len(district_rows) - staffed} \"\n          f\"disconnected={sum(1 for r in district_rows if not r['contiguous'])} \"\n          f\"moved={sum(v['moved'] for v in record.values())} \"\n          f\"band_violations={sum(len(v['band_violations']) for v in record.values())}\",\n          flush=True)\n    for bundle in sorted(record):\n        for row in record[bundle][\"unrepaired\"]:\n            print(f\"  {row['district']}: {row['pieces']} pieces, \"\n                  f\"{row['detached_zips']} zip(s) detached -- {row['reason']}\", flush=True)\n    for c in channels.CHANNELS:\n        print(f\"{c}: {assigned[c]} zip(s) assigned, residual mass {residual[c]:.6g}\",\n              flush=True)\n    print(f\"wrote {out}\", flush=True)\n    return 0\n\n\ndef main(argv=None) -> int:\n    args = build_argparser().parse_args(argv)\n    return _main(args)\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())","lineStart":1000,"lineEnd":1456,"totalLines":1456,"truncated":false}},"title":"tools/plan_realise.py","time":{"start":1789350765512,"end":1789350765517}},"id":"prt_09d9de25c001DKzXu7u0SEC4Vb","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","messageID":"msg_09d9cd028001yqIlIiXQ08u5x5"}}
{"type":"step_finish","timestamp":1789350765770,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"id":"prt_09d9de4c8001r5I4gWgExz88ue","reason":"tool-calls","snapshot":"ebf78eaacb0d4e99993cdfb8a39541532200ae61","messageID":"msg_09d9cd028001yqIlIiXQ08u5x5","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","type":"step-finish","tokens":{"total":108572,"input":1608,"output":91,"reasoning":6777,"cache":{"write":0,"read":100096}},"cost":0.016862032}}
{"type":"step_start","timestamp":1789350767502,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"id":"prt_09d9deb8d001l0d2vOZgUedNK5","messageID":"msg_09d9de539001mt2McSt7XrbbOy","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","snapshot":"e60682ace68cddfd4cb25c5f54dde33c8f0eb07a","type":"step-start"}}
{"type":"tool_use","timestamp":1789350801034,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"type":"tool","tool":"read","callID":"call_00_Km4csZs2dmt1AH8iSiGn7764","state":{"status":"completed","input":{"filePath":"/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/td/model.py","limit":60},"output":"<path>/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/td/model.py</path>\n<type>file</type>\n<content>\n1: \"\"\"nway.py -- N-way division primitives: 3+ candidate owners per zip.\n2: \n3: The two-player contract in `base.py` encodes an allocation as `to_a: set` -- membership is\n4: the whole answer, because there are exactly two sides.  When a zip can be claimed by three or\n5: more reps that representation runs out, and so do `gains`, `objective`, `pieces` and\n6: `fairness`, all of which are written against an `a` side and its complement.\n7: \n8: This module is the N-way replacement for those primitives.  It is deliberately *standalone*:\n9: `base.py` is the frozen harness contract and a serial-only file (CLAUDE.md), so the N-way\n10: maths gets proved out here first and the contract extension lands once, later, with the\n11: reduction test already passing.  See `research/contiguity/NWAY.md` for the design and phasing.\n12: \n13: The model\n14: ---------\n15: Per-rep booked production `S[i][z]`, total booked `T_z = sum_j S[j][z]`, opportunity `M_z`::\n16: \n17:     u_i(z) = c1*S_i(z) + c2*(T_z - S_i(z)) + lam*M_z\n18:     c1 = 1 - lam,   c2 = theta*(1 - lam)\n19: \n20: The inheriting rep keeps `c1` of their own book and captures `c2` of everyone else's.  At two\n21: reps with S = {a: A_z, b: B_z} this is identically `base.utilities` -- asserted by\n22: `tests/test_nway.py::test_two_rep_reduction`, which is what lets the existing corpus of\n23: two-player results stay interpretable.\n24: \n25: An allocation is a `to_owner: dict[node -> rep]`, total over the nodes considered.\n26: \"\"\"\n27: from __future__ import annotations\n28: \n29: import math\n30: from typing import Iterable, Optional\n31: \n32: import networkx as nx\n33: import numpy as np\n34: \n35: # Attribute names.  Native N-way nodes carry `cand` + `S`; two-rep nodes carry the legacy\n36: # rep_a/rep_b/A/B and are read through `candidates` / `books` below.\n37: CAND = \"cand\"\n38: BOOK = \"S\"\n39: FREE = \"S_free\"          # book at z with no incumbent rep (vacancy filler); never a candidate\n40: \n41: # How an inheriting rep capitalises unowned book.  The coefficient multiplies S_free in\n42: # `utilities`; see NWAY.md 6.7 for why this is a decision and not a default.\n43: FILLER_CAPTURE = (\"theta\", \"full\", \"opportunity\")\n44: \n45: \n46: # ------------------------------------------------------------------ schema shim\n47: def candidates(G, z) -> tuple:\n48:     \"\"\"Candidate owners of zip `z`, as a tuple, for native and legacy two-rep nodes.\n49: \n50:     May be empty (untapped, or vacant with only filler book) or a single rep (uncontested).\n51:     The \">= 2 candidates\" requirement belongs to the *subproblem* a solver decides, not to\n52:     the schema -- `is_contested` is the predicate for that.\n53:     \"\"\"\n54:     d = G.nodes[z]\n55:     if CAND in d:\n56:         c = tuple(d[CAND])\n57:         if len(set(c)) != len(c):\n58:             raise ValueError(f\"zip {z!r} has duplicate candidates {c!r}\")\n59:         return c\n60:     return (d[\"rep_a\"], d[\"rep_b\"])\n\n(Showing lines 1-60 of 357. Use offset=61 to continue.)\n</content>","metadata":{"preview":"\"\"\"nway.py -- N-way division primitives: 3+ candidate owners per zip.\n\nThe two-player contract in `base.py` encodes an allocation as `to_a: set` -- membership is\nthe whole answer, because there are exactly two sides.  When a zip can be claimed by three or\nmore reps that representation runs out, and so do `gains`, `objective`, `pieces` and\n`fairness`, all of which are written against an `a` side and its complement.\n\nThis module is the N-way replacement for those primitives.  It is deliberately *standalone*:\n`base.py` is the frozen harness contract and a serial-only file (CLAUDE.md), so the N-way\nmaths gets proved out here first and the contract extension lands once, later, with the\nreduction test already passing.  See `research/contiguity/NWAY.md` for the design and phasing.\n\nThe model\n---------\nPer-rep booked production `S[i][z]`, total booked `T_z = sum_j S[j][z]`, opportunity `M_z`::\n\n    u_i(z) = c1*S_i(z) + c2*(T_z - S_i(z)) + lam*M_z\n    c1 = 1 - lam,   c2 = theta*(1 - lam)\n\nThe inheriting rep keeps `c1` of their own book and captures `c2` of everyone else's.  At two","truncated":true,"loaded":[],"display":{"type":"file","path":"/Users/Shared/sv-ntlee/repos/td/.claude/worktrees/agy-math-review/td/model.py","text":"\"\"\"nway.py -- N-way division primitives: 3+ candidate owners per zip.\n\nThe two-player contract in `base.py` encodes an allocation as `to_a: set` -- membership is\nthe whole answer, because there are exactly two sides.  When a zip can be claimed by three or\nmore reps that representation runs out, and so do `gains`, `objective`, `pieces` and\n`fairness`, all of which are written against an `a` side and its complement.\n\nThis module is the N-way replacement for those primitives.  It is deliberately *standalone*:\n`base.py` is the frozen harness contract and a serial-only file (CLAUDE.md), so the N-way\nmaths gets proved out here first and the contract extension lands once, later, with the\nreduction test already passing.  See `research/contiguity/NWAY.md` for the design and phasing.\n\nThe model\n---------\nPer-rep booked production `S[i][z]`, total booked `T_z = sum_j S[j][z]`, opportunity `M_z`::\n\n    u_i(z) = c1*S_i(z) + c2*(T_z - S_i(z)) + lam*M_z\n    c1 = 1 - lam,   c2 = theta*(1 - lam)\n\nThe inheriting rep keeps `c1` of their own book and captures `c2` of everyone else's.  At two\nreps with S = {a: A_z, b: B_z} this is identically `base.utilities` -- asserted by\n`tests/test_nway.py::test_two_rep_reduction`, which is what lets the existing corpus of\ntwo-player results stay interpretable.\n\nAn allocation is a `to_owner: dict[node -> rep]`, total over the nodes considered.\n\"\"\"\nfrom __future__ import annotations\n\nimport math\nfrom typing import Iterable, Optional\n\nimport networkx as nx\nimport numpy as np\n\n# Attribute names.  Native N-way nodes carry `cand` + `S`; two-rep nodes carry the legacy\n# rep_a/rep_b/A/B and are read through `candidates` / `books` below.\nCAND = \"cand\"\nBOOK = \"S\"\nFREE = \"S_free\"          # book at z with no incumbent rep (vacancy filler); never a candidate\n\n# How an inheriting rep capitalises unowned book.  The coefficient multiplies S_free in\n# `utilities`; see NWAY.md 6.7 for why this is a decision and not a default.\nFILLER_CAPTURE = (\"theta\", \"full\", \"opportunity\")\n\n\n# ------------------------------------------------------------------ schema shim\ndef candidates(G, z) -> tuple:\n    \"\"\"Candidate owners of zip `z`, as a tuple, for native and legacy two-rep nodes.\n\n    May be empty (untapped, or vacant with only filler book) or a single rep (uncontested).\n    The \">= 2 candidates\" requirement belongs to the *subproblem* a solver decides, not to\n    the schema -- `is_contested` is the predicate for that.\n    \"\"\"\n    d = G.nodes[z]\n    if CAND in d:\n        c = tuple(d[CAND])\n        if len(set(c)) != len(c):\n            raise ValueError(f\"zip {z!r} has duplicate candidates {c!r}\")\n        return c\n    return (d[\"rep_a\"], d[\"rep_b\"])","lineStart":1,"lineEnd":60,"totalLines":357,"truncated":true}},"title":"td/model.py","time":{"start":1789350801028,"end":1789350801032}},"id":"prt_09d9e6d20001uTRb25dIdvueBn","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","messageID":"msg_09d9de539001mt2McSt7XrbbOy"}}
{"type":"step_finish","timestamp":1789350801259,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"id":"prt_09d9e6f690013LNHszANnS50Aa","reason":"tool-calls","snapshot":"d3d70b0c5d96229d4b3411810284cc8ab05d5a4b","messageID":"msg_09d9de539001mt2McSt7XrbbOy","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","type":"step-finish","tokens":{"total":120350,"input":8248,"output":87,"reasoning":3471,"cache":{"write":0,"read":108544}},"cost":0.014876488}}
{"type":"step_start","timestamp":1789350803273,"sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","part":{"id":"prt_09d9e77470012emGRAjJVWQ5Xs","messageID":"msg_09d9e6fd9001PklkTAzUp68GyO","sessionID":"ses_f6265cc4bffeSiQIPNxNxw1wKd","snapshot":"502cc10d2e399cb2aa3888aba8542006f34c0704","type":"step-start"}}
